# Feature Flows Index

> **Purpose**: Maps features to detailed vertical slice documentation.
> Each flow documents the complete path from UI → API → Database → Side Effects.
>
> For detailed change history, see `git log`.

> **Scope — the doc follows the code.** This index covers flows whose
> implementation is OSS code in this repository, regardless of which tracker the
> issue was filed in. Flows for code that lives in the optional private
> submodule (`src/backend/enterprise/`) are documented in that repository, not
> here; see [docs/ENTERPRISE.md](../ENTERPRISE.md) for how the seam works.
> Public docs never carry private-module design or schema
> (trinity-enterprise#45, enforced by `.github/workflows/enterprise-docs-guard.yml`).

---

## Recent Updates

> Newest ~20 changes only — older entries are discoverable via the **Documented Flows**
> category tables below, the linked flow doc, and `git log`. (Capped per #1360 to keep
> this index minimal and navigable.)

| Date | ID | Change | Flow |
|------|-----|--------|------|
| 2026-08-17 | #2258 | fix(workspace): **"Sign out" signs out — for both ways of being signed in**. `signOut()` cleared only the portal token, and since ent#357 a platform JWT is an implicit workspace session (`isPlatformSession = !portalToken && isAuthenticated`), removing the portal token was precisely what *activated* the fallback — a refresh re-entered as the operator; a client on a browser also holding a platform login re-authenticated as that operator. Fix = **destroy the credential, never a derivation of it**: `store.signOutEverywhere()` ends the platform session (`authStore.logout()`) FIRST, then clears portal state, and routes by principal (operator → `/login`, client → the OTP form). The issue's suggested persisted suppression flag was rejected on evidence: the JWT is an axios DEFAULT header and per-request headers merge over defaults, so a flag hides the disclosure while every portal request still carries the operator's JWT; one bare-`fetch` path would 401 while axios paths stayed authorized; a wrong OTP digit would trip the global bounce onto the operator `/login`. `auth.logout()` now clears local state BEFORE the network revoke (the 401 interceptors and the `/login → /` guard both key on it — NavBar's un-awaited `logout(); push('/login')` landed on the dashboard since #187). `endSession({expired})` deliberately does NOT end a platform session. One footer button, labelled per principal ("Sign out of Trinity" / "Sign out"). Residuals stated: client-session expiry with a later platform login; portal token server-side validity (no self-service revoke; per-email primitive exists, ent#281). | [workspace-session-signout.md](feature-flows/workspace-session-signout.md) |
| 2026-08-16 | #1920 | fix(reliability): **hand-rolled Redis single-flight locks consolidated onto ONE ownership-checked primitive**. The SETNX single-flight idiom was copied into seven sync sites with divergent behaviour, and the #1919 bug class (extend/release a lock you no longer own) was live in three of them — worst in `system_seed_service`, whose release did an **unconditional, tokenless `r.delete(...)`** that can delete a *successor's* lock (a slow first-run pass whose 600s TTL lapsed while a second worker re-took the key). `cornelius_agent_service` and `compat_fix` (`services/compatibility/fixes.py`) were found to carry the **same** constant-`"1"` value + tokenless-delete bug verbatim and are fixed with it. New `redis_breaker_util.SingleFlightLock` (leaf module, sibling of the circuit-breaker plumbing): mints a **unique per-acquire `uuid` token**, `acquire()` / `refresh_if_owned()`→`LeaseState{OWNED,REACQUIRED,LOST,DEGRADED}` / `release_if_owned()`, **fail-open** (None client / Redis error → sole-worker, `held` stays False so refresh/release no-op), single-use state machine (`_held` set once, never cleared — a DEGRADED refresh must not abandon a live lock for the TTL, the ops 2100s-wedge class). Release is GET-then-DELETE **compare-and-delete** — deliberately non-atomic (the accepted TOCTOU window `ops.py` already shipped) so it stays fakeredis-unit-testable; the async Lua-CAD `ResumeLock` stays the stronger, **deliberately-separate** primitive. Client is **injected** (no helper-owned factory) so every existing per-site `get_breaker_redis` monkeypatch keeps biting. Adopted by `ops.py` (fleet-restart, keeps `refresh_if_owned`), `ephemeral.py` (discard lock — the quota SEED stays a legitimate non-lock nx use), `skill_service` ×2, `system_seed_service` ×2 (the `system_seed:provision` lock **and** the #2215 `first_run_seed:provision` pass lock, which landed on `dev` while this was open), `cornelius_agent_service`, `compat_fix`. Deliberate **non-consumers** preserved: `ResumeLock`, the `monitoring`/`operator_queue` leader leases (stable cross-cycle worker id), canary's Lua-CAD leader lease. Anti-recurrence: a static guard (`tests/unit/test_1920_no_hand_rolled_single_flight.py`) fails CI when a new hand-rolled `set(..., nx=True, ex=...)` single-flight lock appears outside `SingleFlightLock` with no allowlist entry, and pins the adopted files clean — it caught two of the three `nx=True` sites that landed on `dev` during the rebase (#2215's pass lock, adopted; #2216's backup lease, allowlisted as consolidation-only since it already carries token + compare-and-delete) and one that must stay hand-rolled (#2215's `port_alloc:{port}` reservation — an allocation, not a mutex: `SingleFlightLock`'s internal fail-open would read a Redis error as *reserved* and hand two agents the same SSH port). The #1920 lock fix is class hygiene, NOT the double-seed guard — system_seed's real convergence guard is the reserved-name existence backstop. No schema change, no migration, no flag (Redis-only). | [ephemeral-agents.md](feature-flows/ephemeral-agents.md), [skill-injection.md](feature-flows/skill-injection.md), [system-manifest.md](feature-flows/system-manifest.md), [cornelius-default-agent.md](feature-flows/cornelius-default-agent.md), [agent-compatibility-validation.md](feature-flows/agent-compatibility-validation.md), [architecture.md](architecture.md) |
| 2026-08-16 | #2216 | feat(backup): **automatic database backups, both backends, zero setup** — the shipped `backup-database.sh` was a never-invoked workstation GCP pull doing a naive live `cp`; a real instance lost its DB to a header overwrite with no recovery point. New `services/db_backup_service.py` (daily 03:30 UTC, backend lifespan): SQLite via to_thread'd stdlib `Connection.backup()` (one-shot consistent snapshot — the platform DB runs DELETE journal mode, verified live), PG via `pg_dump -Fc` (full conninfo URI, only the password stripped to `PGPASSWORD` env; `postgresql-client-17` major-pinned in the backend image; timeout kill+reap+unlink). Day-keyed artifacts + fail-open SETNX lease (duplicate-I/O suppression, never a correctness boundary). New leaf `db/backup_primitives.py` also hosts the **boot pre-migration copy** (SQLite, pending-gated, fail-open, fresh-install SILENT vs corrupt-DB ERROR as distinct branches; the incident's migration-runner fingerprint is provably unchanged). Retention **inverts the #1638 fail-safe direction**: `backup_retention_days` (ops model, 14, bounds **1**–3650, `0` INVALID = keep-forever trap) + fixed `BACKUP_MIN_KEEP=3` floor; prune runs on EVERY attempt (no only-after-success Catch-22); free-space preflight skips loud, never prunes-to-make-room; ONE shared reader (garbage → 14, never → 0) and the retention GET excludes the key from the generic windows map. Durable `system_settings` status + `backup` block on `GET /api/settings/retention` (`scope: same-disk`); edge alarm on ok→failure + 3d/weekly staleness re-alarm on sentinel `_db-backup` (`db-backup-` reserved prefix; canary L-03 sentinel exclusion generalized to a tuple). Env forwarded in BOTH compose files. Legacy scripts fixed (pull newest artifact; restore stops backend AND scheduler + removes stale sidecars); user guide rewritten; AC#5 restore exercised by test. | [database-backup.md](feature-flows/database-backup.md) |
| 2026-08-16 | #2217 | feat(canary): **run-state observability** — nothing reported whether the harness was running, and a disabled canary emits zero violations, byte-for-byte identical to a clean fleet. This is the **H-01 class one level up**: H-01 catches a blind collector *while a cycle runs*; it structurally cannot catch "no cycle is running at all" (a dead loop emits nothing). New admin-only `GET /api/canary/status` → `CanaryService.get_run_status()` (Invariant #1) returns `{enabled, status: disabled\|healthy\|stale\|unknown, last_cycle_at, seconds_since_last_cycle, interval_seconds, stale_after_seconds, alert_sink_configured, redis_available}`. Reads the **shared** `canary:last_cycle_at` cursor (never the per-worker `self.last_run_at`/`cumulative_cycles`, stale/zero on a non-leader under #1881's lease). The cursor is written at cycle END carrying `snapshot.snapshot_time` — the collection-**start** instant — so it lags real completion by up to one cycle's duration; `/status` reports that instant and derives staleness from its age. **Fail-open so default-OFF never alarms**: `disabled` short-circuits before Redis (`redis_available=None`); any Redis/parse failure is `unknown` — never `stale`. Threshold `stale_after_seconds = _max_failover_seconds() + _MAX_CYCLE_LEASE_SECONDS` (≈1680s), both the service's own constants so it can't drift out of step — provably above BOTH the ~780s failover window AND a maxed-healthy ≤900s cycle (whose collection-start-instant cursor age reaches `interval + cycle_duration`); `_max_failover + interval` (1080s) would false-`stale` a slow-but-healthy cycle. `alert_sink_configured` (is `CANARY_SLACK_WEBHOOK_URL` set) is a **separate** field — a cycling canary with no sink pushes nothing, a silent-green that must not read as unqualified `healthy`; liveness vs can-it-alert are orthogonal. Secondary: a boolean `canary_enabled` on `GET /api/settings/feature-flags` (observability-only flag home, any authed user; detail stays admin-only), backed by public `CanaryService.is_enabled()`. Backend-agnostic since #1540 (safe on PostgreSQL); the re-enable itself is a pure ops toggle (compose wiring shipped #1876). A manual `POST /run-cycle` also advances the cursor, so `/status` is last-cycle across scheduled AND on-demand. An **active push** liveness alarm is a deferred follow-up (needs its own default-OFF gating). No schema change, no migration, no new config knob, no compose change. Guard: `tests/unit/test_2217_canary_status.py` | [architecture.md](architecture.md) |
| 2026-08-16 | #1710 | refactor(auth): **retire the `slack.py` INV-8 carve-out** left by #1310. The 11 inline `if not db.can_user_*_agent(...): raise 403` sites (each carrying a `# noqa: inv8` waiver) migrated onto the shared imperative guards — `assert_agent_access` (3 read) / `assert_agent_owner` (8 owner) in `dependencies.py`. **Behavior-preserving** (AC #6): same 403 + `detail` string byte-for-byte; every site was already access-first so no 404-before-403 enumeration oracle is introduced (AC #5); the helpers add `_enforce_connector_scope` first, redundant belt-and-braces since a connector key is already 403'd at the auth entry point before reaching any slack handler. Site #10 (proactive-consent toggle) keeps its ent#223 human-only `reject_agent_principal` line — `assert_agent_owner` is agent-permissive. **Zero** `# noqa: inv8` markers remain in-tree; the static guard `test_1310_auth_wiring` still trips a re-introduced inline gate. Behavioral proof added to `test_1310_auth_consolidation` (A9: stranger→403, shared-reader read/owner matrix, site-#10 human-only vs site-#11 send asymmetry) + a runtime ordering proof in `test_223_slack_channel_proactive_consent`. IDOR/public-link/rate-limit logic untouched. | [slack-integration.md](feature-flows/slack-integration.md), [role-model.md](feature-flows/role-model.md) |
| 2026-08-16 | #1704 | feat(plugins): **an agent's Claude Code plugin selection is a declared, committed, self-healing manifest**. **Reframe first**: the issue's premise ("a recreate loses plugins") is not reproducible — HOME (`/home/developer`) IS the durable `agent-{name}-workspace` volume, no recreate removes it, and startup.sh preserves untracked files, so a plain recreate keeps `~/.claude.json` and the `~/.claude/plugins/` cache both. The real gap is a **git-based reconstitution** onto a fresh/empty volume or a new host (the #1169 move exports `data/` only; the #834/#1581 hard-purge removes the volume) — a gap #1705 completed by removing the last incidental crutch (the cache used to be auto-committed). Mechanism = the agent-local half of trinity-enterprise#192: a `template.yaml plugins:` block → total tolerant reader `services/template_plugins.py` (the ent#89 shape; both catalog builders surface `plugins`+`plugin_errors`) → `_TemplateResolution.declared_plugins` fed by **all three** resolver branches (github source metadata / local `template_data` / copy snapshot — NOT `template_data`, which is `{}` on the github: path) → `git_service.materialize_plugins` writes nested `~/.trinity/plugins.yaml` via the shared injection-safe heredoc writer (`sort_keys=True` for byte-stability so the 15-min auto-sync loop never re-commits a churning manifest), opt-in + ghost-skipped + non-fatal in the rollback fence. **Committed** via `_TRINITY_AUTHORED_PATHS` (the #2070 `!` re-include + rm-cached exemption) so it survives the reconstitution — while `.claude.json`/`.claude/plugins/` stay gitignored (**#1705 intact**, never un-ignored). **Boot self-heal**: `startup.sh` runs `agent_server.plugins_reinstall` AFTER credential injection (a private marketplace needs a git credential — resolved from `GITHUB_PAT` env, never the manifest), hardened-parses the untrusted manifest (`AliasPolicy.REJECT`), re-charset-validates every name and the marketplace **`source`** (the dangerous arg — refuses `user:token@` userinfo/traversal/leading `-`), reads current state via `claude plugin [marketplace] list --json` and installs only what's missing (arg-lists, hard `timeout`+`stdin=DEVNULL` so a no-TTY prompt can't hang) — **zero subprocesses** when present. Base-image change → old images silently skip the hook (release-note ordering). **Deferred**: capturing runtime `/plugin install`s (a distill of Claude's undocumented moving internals) and a commit-pinned (`auto_update: off`) mode. | [agent-plugin-manifest.md](feature-flows/agent-plugin-manifest.md), [github-sync.md](feature-flows/github-sync.md), [agent-data-volumes.md](feature-flows/agent-data-volumes.md) |
| 2026-08-16 | #1853 | fix(execution): **persist telemetry AND the transcript on a failing `error_during_execution` (502) / timeout (504) row**. The whole failure class was undiagnosable — the row carried status + error string only, no `claude_session_id`/`cost`/`execution_log` (measured: 26 SUCCESS rows carried log+session_id+cost, 6 FAILED rows carried none) — the observability substrate for the Aug-14 "system agent identifies errors" initiative (TOWARD the #1401 recovery trace). Two structural gaps: the agent's `execution_error` 502 raised a bare `detail="Execution error: <msg>"`, discarding `ctx.metadata` + `ctx.raw_messages` in scope; and `apply_result`'s FAILED branch never passed `execution_log`/`claude_session_id` (cost/context already salvaged). **Approach B — inline the sanitized transcript** (SUCCESS already inlines the full transcript on *every* run, so inlining on the rarer failures is strictly cheaper; retrievable via the same API, survives stop+delete under the 30-day retention window; captures short `≤600s` runs from in-memory `raw_messages`, no JSONL dependency). New `_execution_error_502_detail` mirrors `_timeout_504_detail` (`{message, metadata, execution_log}`) with a **UUID-validated** `session_id` fallback (`_valid_session_id`, FI-1: `ctx.claude_session_uuid` can be an untrusted `resume_session_id`); `_timeout_504_detail` gains the same. `message` text is byte-identical (preserves #1938 + the resume-not-found self-heal); the body does NOT trip `_is_reader_race_signature` (no false #678 retry). `_extract_agent_error` returns the transcript (3-tuple); the FAILED branch of `apply_result` mirrors the SUCCESS branch (`sanitize_execution_log` redaction + #1741 `tool_calls` summary + `claude_session_id`), added to the **existing** `db.update_execution_status` call **above** the `if won` gate. **Single terminal applier (#1483) preserved** — no new CAS writer, `_write_terminal_and_gate` untouched, side-effects stay `won`-gated (#1578/#1804). No schema change. Also corrects the stale "reaps after 24h" comment (real: 1–7h). **Residual:** `_write_terminal_and_gate` (backend timeout/budget/crash) + standalone-scheduler RETRY-001 FAILED writes still land bare. Base-image rebuild + cold recreate for the agent half (#1809); old-image bodies degrade gracefully (columns stay null). 21 tests | [task-execution-service.md](feature-flows/task-execution-service.md), [parallel-headless-execution.md](feature-flows/parallel-headless-execution.md) |
| 2026-08-14 | #2127 | fix(agent-runtime): **a fan-out turn is no longer killed mid-wait and booked as SUCCESS**. `claude --print` emits one `{"type":"result"}` per turn *segment* and deliberately stays alive to await background subagents/workflows — they are exempt from the ~5s background-shell grace "because their result is part of the final output" — but #970's early-completion set `result_seen` on **any** result line and SIGTERMed the process group 2s later with `return_code = 0`. So the CLI was killed *during a wait it was performing correctly*: the run recorded **success** with the model's "I'll wait for the notification" announcement stored as its response, cost billed, subagents killed, nothing logged. **The issue's own stated root cause was wrong** — it blamed the result classifier for trusting a clean `end_turn`, but the classifier never runs, and building its suggested transcript-scanner would have been a detector layered on our own kill. **#1870 is the same defect from the other end** (fast notification → interrupted follow-on turn → `error_during_execution`), i.e. one bug filed twice. Root-caused by instrumenting Claude Code 2.1.220 in a live agent container: first result t+37.82s, real answer t+40.32s, exit t+40.72s — the kill lands ≤t+39.82s. Fix requires **all three** of `result_seen`, an empty ledger, and a quiet stream. Ledger half: `system/background_tasks_changed` carries a full snapshot of in-flight work (`[A]→[A,B]→[A,B,C]→[B,C]→[B]→[]`), so the latest event is the whole truth. **Idle half — not optional**: the ledger alone misses the reported shape, because once the fan-out ends the ledger drains to `[]` while the model composes the synthesis (measured: 5 subagents done by t+27.8s, real `FINAL REPORT` at t+55.9s), so a ledger-only gate lets the kill land on `"ECHO reported."`. Idleness is what #970's failure *is* — claude alive, blocked on an MCP child, silent. `_IDLE_FINALIZE_S`=**300s** (`AGENT_IDLE_FINALIZE_S`, `<=0` refused), set from the worst silence a healthy run makes: without `--include-partial-messages` one long message is one stdout line emitted only when complete — **40.54s of dead air for 9,840 chars**, vs a 7.33s worst gap across three fan-out runs — so ~7.4× headroom, biased large because too-large only delays the pathological #970 finalize while too-small destroys a deliverable and calls it success. Free on the happy path: the branch is only reachable when the process does **not** exit. `_NON_WAITED_BG_TASK_TYPES = {local_bash, local_shell}` is a **denylist** because the failures are asymmetric — an allowlist that hasn't heard of a future waited type restores the bug *silently*, a denylist miss costs a bounded wait — and a background shell must never hold the finalize, since the CLI kills it after ~5s (measured 5.09s) and gating on one re-opens #970's lingering-child class. Malformed ledger ⇒ 0 (degrades to pre-fix behaviour, never a new hang class); the read takes `task_type` only, never `description`/`prompt`/`summary` (subagent prompt text and output). `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS` derived from the execution timeout at **both** spawn sites — the chat path never had the kill but inherits the same 10-minute CLI truncation, and fixing only the reported path is the recurring one-of-two-call-sites escape (#686, #1264). When the CLI stops waiting anyway, finalize prepends a notice + sets `metadata.background_tasks_pending_at_exit`, after the #160 empty-response branch so it can't make an empty result look populated. ⚠️ Consequence: fan-outs now run to completion, so they spend more and can newly reach `--max-turns`. Base-image rebuild + cold recreate required. 20 tests, asserting **end state** (the late answer survives, not the placeholder) and verified failing without the gate | [parallel-headless-execution.md](feature-flows/parallel-headless-execution.md) |
| 2026-08-13 | #925 | feat(ui): **client-side cron validation + invalid-schedule warning icon** — the schedule form pre-validates as the user types via `utils/cronValidation.js`, a zero-dependency hand-rolled mirror of the backend's EXACT grammar (`schedule_validation.py` 5-field split + verbatim `_dow_to_apscheduler` port + APScheduler 3.11 field rules, incl. the prefix-match name expressions, the step-span rule and the Python-truthiness `last or MAX` fallback — JS `\|\|` on purpose, not `??`); cron libs were rejected on ≥8 proven verdict disagreements. Contract = `tests/fixtures/cron-grammar-cases.json` (111 rows PROBED against the live validator, never hand-typed) asserted row-for-row by BOTH `tests/unit/test_925_cron_grammar_fixture.py` (the drift alarm in the backend CI env) and the vitest spec — an APScheduler bump that moves the grammar fails CI loudly instead of desynchronizing the mirror. Form: the format-hint line doubles as the reserved error slot (no modal jump; every message shape measured ≤2 lines at the modal's real 400px width); submit disabled ONLY when non-empty-AND-invalid (empty keeps the native `required` bubble); editing shows an invalid stored cron unconditionally so a dead Update button is never unexplained. List: warning triangle inside the cron chip, tooltip exactly "Invalid cron expression". Fail-open (`{valid:true}` on internal error) — the backend 400 stays the authority; backend runtime untouched | [scheduling.md](feature-flows/scheduling.md) |
| 2026-08-12 | ent#100 | feat(grid): **Recent failures info tile** — the first DATA tile on the ent#325 widget chassis: newest failed executions fleet-wide plus the 24h total, from the EXISTING `GET /api/executions?status=failed` + `/api/executions/stats`, ridden on the one 60s visibility-aware batch poll and gated on the tile being enabled. **Zero backend change, no new endpoint, no schema change, no new timer** — the tile never fetches, because viewport culling *unmounts* tiles and an `onMounted` fetch would re-issue on every pan. The green "No failures in 24h ✓" is treated as a POSITIVE claim needing positive evidence and is unreachable from all three faults that would otherwise manufacture it: a failed rows GET (#1926), a failed `/stats` GET (the 24h total is a second request, so its failure means UNKNOWN, not zero — hence one `{data,loaded,error}` triple per GET, never shared), and an **unenumerable fleet** (`accessible_agent_names` → `list_all_agents_fast()` returns `[]` on ANY Docker fault, so a non-admin gets HTTP 200 + zeros — the ent#384 hazard, closed client-side by requiring a non-empty roster). A *refresh* failure over loaded rows still stays `ready`; only the CONFIRMATION needs everything green. Named AC deviation: **no error-code taxonomy** — `schedule_executions` has no `error_code` column, so `failureCodeFromSummary` READS an anchored lower-case `[code]` marker when the platform emitted one and returns null otherwise; a JS re-classifier was rejected as a third unenforced mirror of the byte-identical `failure_classifier.py` pair guessing at a 200-char truncation. Chassis fixes: info tiles get the **unfiltered** roster (`orgAgents || agents`) so the ent#261 type-to-filter can't degrade fleet-tile labels to raw slugs per keystroke; the shared 1s tick only reaches catalog entries declaring `wantsTick`; `InfoTile` sets `inheritAttrs: false`. Both tile guards were narrower than their own stated job and were widened: `gridTokens` now auto-discovers tiles recursively (it was a hand-written two-entry list) and `gridTileLinks` now brace-balances `to:` object literals built in `<script setup>` (it matched only a literal `link-to` ATTRIBUTE, so every per-row link was invisible — on the guard whose failure mode is a frozen dashboard). Sibling **Next schedules** (ent#99) deliberately HELD; its motivating `ORDER BY next_run_at` static guard ships anyway, because that latent bug exists today | [dashboard-grid-view.md](feature-flows/dashboard-grid-view.md) |
| 2026-08-12 | #2137 | refactor(compat): **align the agent-compatibility catalog with what the platform implements and the marketplace generates**. 101 checks -> 88; a `create-agent`-scaffolded agent went from **9 findings (6 SOFT + 3 INFO)** — none the author's fault, none actionable — to **0 HARD, 0 SOFT, 5 INFO**. Three classes. (1) **Gating on fields nothing reads**: `template.yaml`'s `git:` block has no backend reader and no bundled template declares it, yet **T-017 was HARD** (+G-003/4/5); same for `metrics:` (D-006) and `.trinity/post-check` (I-005), whose only other mention was a `git_service` comment pointing back at I-005. The post-check PATH stays in `_TRINITY_AUTHORED_PATHS` — #2070 derives the `!` re-includes from it and 14 templates ship `!.trinity/post-check`. (2) **Failing every agent we ship**: `_slash_command()` anchored at position 0, but the marketplace writes `"Run /pipeline-tick"` — so **P-006, the HARD guard against a scheduled run hanging on an approval gate, had never fired**; `_command_names()` globbed `.claude/commands/` only, the one layout the wizards never produce, so X-007 false-failed skill-based agents; G-002 compared against the 58-entry fleet-wide list **Trinity itself injects** (`.bashrc`, `.ssh/`, …). (3) **Self-contradiction**: P-009 says split `SKILL.md`, P-004 then flagged the resulting `reference.md`. Plus: a "duplicate" is not always a severity duplicate — `c_k002` was literally `return c_t015(snap)` but K-002 was **HARD** and T-015 **SOFT**, so retiring it would have silently downgraded the credential gate and undone ent#128; **T-015 promoted to HARD**, caught by that issue's own test. P-006 now honours the marketplace `automation: gated|manual` opt-out (an intentional pause is a design choice). And the anti-drift test **did not hold**: `test_ids_match_doc` matched a SINGLE-letter `[A-Z]-\d{3}`, so `DP-001`..`DP-005` sat documented, indexed and unimplemented while the test reported the files in sync — regex widened, `DP-001..004` implemented (DP-001 shares `_is_safe_data_path` with the materializer), DP-005 retired, and a new `test_severities_match_doc` guards the column an operator actually reads. Retired ids are never reissued so persisted `checks_json` stays interpretable. | [agent-compatibility-validation.md](feature-flows/agent-compatibility-validation.md) |
| 2026-08-09 | #1897 | fix(canary): **a rejected Slack post is undelivered, and gets retried**. `CanaryAlerts.emit_transition` was `-> None` and dropped `post_webhook`'s `success`, so the caller read "did not raise" as "delivered": it counted the transition, advanced `canary:last_cycle_at`, and continuing-red suppression then silenced that invariant **forever** — the alert unrecoverable while `cumulative_transitions` reported it sent. Fix = `AlertResult(outcome, error)` over a 3-state `AlertDelivery`; SKIPPED is deliberately **not** FAILED (no webhook configured is the default install, and folding it in would arm a retry per transition and end in a give-up ERROR on a correctly silent sink), and the caller withholds the count only on an **explicit** FAILED (`outcome is not AlertDelivery.FAILED`) so a stub returning a bare enum or `None` keeps today's reading. Retry state is per-invariant in the Redis hash `canary:alert_pending`, **not** the cycle-global cursor — withholding that cursor retries nothing (the invariant's own freshly-inserted row already post-dates it) while silently swallowing an unrelated red→green→red flip, which is why the issue's own proposed fix was rejected, on a corrected reason. The entry is armed **BEFORE** the POST and `HDEL`'d on success: `asyncio.CancelledError` is not an `Exception` and `stop()` cancels a live cycle, so arm-on-failure loses the alert on every deploy landing inside the 5s webhook await (removing the pre-await write turns exactly one test red). Bounded by **age**, not attempts — `MAX_ALERT_PENDING_AGE_SECONDS` (1800) per contiguous failure run, with a 3-interval run decay so brief flaps can't spend a later outage's window and a one-interval retry floor so the ungated manual `run-cycle` can't burn it; because the budget is checked *after* each attempt (so the ERROR quotes a real elapsed and a real `last_error`), a run ends on the first attempt whose age *exceeds* the window: **8 POSTs over 2100s** at the 5-min default, not 6 over 30 min. ⚠️ **API contract change:** `RunCycleResponse.transitions` now means *delivered*, not *detected* — `undelivered_invariant_ids` rides beside it so a webhook outage can't render as a green cycle, and `cumulative_transitions_detected` preserves the detection metric. Everything fails open to exactly the pre-#1897 behaviour, and the record is parsed per field so one corrupt value can't disable retry fleet-wide. No schema change, no migration, no flag (Redis-only) | [architecture.md](architecture.md) |
| 2026-08-06 | ent#15 | feat(agents): **GitHub-repo import intents — fork / copy / clone, with inline compatibility check**. `POST /api/agents` (+ MCP `create_agent`, UI Create-Agent modal) accepts `import_intent`: `fork` = ent#93 fork-to-own unchanged (block required — 400 `FORK_PARAMS_REQUIRED` without it; a stray block with explicit copy/clone is 400 `INTENT_FORK_BLOCK_CONFLICT`, since presence-triggered forking silently creates a GitHub repo); `clone` = legacy default unchanged; `copy` = **backend-materialized snapshot** — staging clone (`--depth 1`, fork_to_own's `_run_git`: `/data` staging, PAT via `GIT_CONFIG_*` never argv, `@branch` honored, empty repo → 400 `COPY_SOURCE_EMPTY`, tree-escaping symlinks pruned) → strip `.git` → pre-populate the workspace volume via deploy-local's `_prepopulate_workspace_from_template` BEFORE the container exists, so the container carries **no GitHub env and no PAT**, no `agent_git_config` row (both rebuild seams pop git env by construction; PAT-drift matcher stably true; never sync-polled), zero base-image dependency (`.trinity-initialized` predates every image). Copy bypasses the ent#123 tokenless source-mode gate (it protects boot-push; copy never pushes). Rollback removes the pre-populated volume AFTER the container reclaim so the #1667 guard can't 409 the retry. Provenance: `AgentStatus.import_snapshot` {repo, branch, SHA} → create-audit details + `trinity.import-intent` label (volume-loss rebuild = empty by design — re-cloning would fetch CURRENT upstream, the #1809 class). Inline compat = the wizard's post-create step polling the EXISTING #668 endpoint, gated on agent-server readiness (`/info` 200 — Docker "running" races the clone and false HARDs would persist via the upsert) with an honest failed-to-start branch. Invariant #18: `Idempotency-Key` on create, scope `agent_create:{user_id}` (two-principal isolation), in-flight 409 `CREATE_IN_FLIGHT`; MCP derives a deterministic key (name included). Fork-scope failures name the copy/clone alternatives — never silent auto-degrade. | [github-import-intents.md](feature-flows/github-import-intents.md) |
| 2026-08-06 | #1881 | fix(canary): **one cycle per fleet, not one per uvicorn worker**. Two chained defects that had to land together. Part 1 (shipped first, as #1876): `CANARY_ENABLED`/`CANARY_SLACK_WEBHOOK_URL` were wired into `docker-compose.yml` only, and prod compose launches **standalone** (no base merge, no `env_file:`) — so on staging/dev, the deployment §31 documents the harness *for*, the `.env` knob was inert and the watcher could not be started at all: the #1039/#1056 packaging class, and a silent-green one level above H-01 that no invariant can catch, since invariants only run inside the thing that isn't running. Part 2 (this change): the lifespan starts `canary_service` in **every** worker and it held only a per-process `asyncio.Lock` — re-entrancy inside one process, nothing about cross-worker exclusion — so turning part 1 on meant two full cycles per interval. Measured on eu2: cycle logs interleaving at ~2m15s/~2m47s alternating gaps (two 5-min loops, offset) and **11,942 `canary_violations` rows in 24h**, with R-01 `docker exec`ing into every running agent container twice per 5 min and two writers on `canary:last_cycle_at`/`canary:last_cycle_red`/`canary:e02:terminal_seen`/`canary:h01:suspect_since`. Fix = a `canary:leader` lease on the scheduled loop (SET NX, own-lease-only refresh, release on `stop()`) mirroring `monitoring:leader` #1464 / `opqueue:leader` #1632. Two deliberate deviations from the precedents, both because a duplicate canary cycle is **not** inert the way a doubled breaker feed or an on-conflict create is: the TTL carries a **floor** (a cycle's cost is R-01's untimed `exec_run` sweep, which scales with *fleet size*, not with how often we look, so a shortened interval must not shorten the lease below one sweep), and the **fail-open** posture is justified on different grounds — this is the subsystem whose job is noticing that something went quiet, so noisy-and-visible beats silent, and a Redis outage is already a state the harness announces (`sources_unavailable`, H-01 firing unconfirmed) that failing closed would suppress. Consequently the lease is **best-effort, not mutual exclusion**: H-01's `CONFIRMATION_MIN_SECONDS` and R-01's `DWELL_SECONDS` elapsed-wall-clock gates stay load-bearing and were re-documented, not relaxed (learnings.md 2026-07-29). `run_cycle()` is **not** gated — the on-demand endpoint lands on an arbitrary worker, and an empty payload there is indistinguishable from a green cycle. Non-leaders log on the leadership **transition** only. No schema/endpoint/flag change | [architecture.md](architecture.md) |
| 2026-08-04 | ent#14 | feat(templates): **remote template registry** — the GitHub half of the catalog is sourced at RUNTIME from a `registry.yaml` fetched over HTTPS, so curating an install's starter fleet is a vendor file edit rather than a Trinity release. Purely **additive**: `DEFAULT_GITHUB_TEMPLATE_REPOS` has been `[]` since #1931, so this fills a branch that is empty today, and an admin-curated list still wins outright — that install never even issues the fetch. **Fail-open is structural, not an `except` branch**: `get_all_templates()` returns `local + github`, `local` needs no network, and the registry can only ADD to `github` — so unreachable / 5xx / timeout / malformed / alias-bombed / oversize / redirected / empty all reduce `github` toward `[]`, the already-shipping default state. Proven mode-by-mode through the real parser and a real transport, asserting on the CATALOG OUTPUT rather than on whether an except branch ran. Entries are parsed into a frozen **four-field allowlist** and never splatted, so a registry cannot assert `fork_to_own`, `credentials`, `schedules`, `data_paths`, `hidden` or `id` — but the requirements entry says plainly that **`repo` is a capability pointer**: choosing the repo chooses which `template.yaml` Trinity fetches and trusts, so the allowlist bounds the direct blast radius, not the indirect one. `AliasPolicy.REJECT` via a named `safe_yaml` helper (and the docstring clause that read as an argument for BUDGET is amended — it means the per-repo payload, not the remote index); the byte cap is enforced on bytes RECEIVED, because `Content-Length` is absent on chunked responses and trivially lied about; `follow_redirects=False` because a validated URL that redirects is an SSRF bypass. Cache TTL is 3600 s + jitter, deliberately UNALIGNED with the 600 s per-repo TTL (aligning is a correlated herd, not a rhythm) with a 7-day serve-stale cap and a cross-worker **generation counter** — a per-process invalidate half-applies under `--workers 2`, and a nondeterministic setting is worse than a slow one. Two findings fixed with it: the registry lands in **both** resolvers, because `get_github_template()` also feeds agent creation and its fallthrough passed no override dict (*the create path is never one call site*); and **`fork_to_own: required` now fails closed** — a rate-limited `template.yaml` read as `None`, so the gate never fired and the agent was bound to the shared upstream template repo instead of a user-owned copy, silently. Also found in passing: `../evil` matches all four copies of the `owner/repo` regex, so the registry gate refuses dot segments and a parity test pins the divergence | [template-registry.md](feature-flows/template-registry.md), [platform-settings.md](feature-flows/platform-settings.md), [library-page.md](feature-flows/library-page.md), [template-processing.md](feature-flows/template-processing.md) |
| 2026-08-02 | ent#127 | feat(credentials): **guided per-agent credential setup** — `GET /api/agents/{name}/credential-requirements` joins the ent#128 declaration against live in-container state and an Agent Detail checklist renders set / missing / where-to-get-it. Status comes from a fixed base64 `docker exec` probe returning **key names with a non-empty value, never values**, projected backend-side onto the declared set; "set" is *defined* as agreement with the agent's own exporter and pinned by a parity test. **`.env` absent ⇒ `missing`, never `unknown`** (a `github:` agent never gets one, and that fleet is AC #3's audience), and **`degraded` dominates the empty state unconditionally** because a degraded lookup and a credential-free agent produce an identical empty list — an EMPTY catalog result counts as degraded, since `get_github_template` returns empty-not-None on a failed fetch. New fourth state `declaration_incomplete` closes the confidently-wrong green (12/25 bundled templates declare `credentials: {}`). Owner-only **AND human-only**: the read gate must equal the write gate it drives, and `AuthorizedAgent` would hand an agent-scoped key the credential inventory of every sibling its owner can access. The exec is bounded three ways (container-side `timeout`, `asyncio.wait_for`, `S_ISREG`) because `execute_command_in_container`'s `timeout` is never forwarded and the call runs on the shared 4-slot Docker pool — `mkfifo .env` is agent-triggerable. `setup_url` renders a UTS-46 nontransitional host with eTLD+1 emphasis, failing **closed** to inert text (the stdlib `str.encode("idna")` codec disagrees with browsers on exactly the deviation set that matters). Writes reuse the existing owner-gated inject path, whose two latent defects are fixed here (a transient read failure wiped every configured credential; the quote round-trip grew one backslash per submit). No DB table, no migration, no flag | [guided-credential-setup.md](feature-flows/guided-credential-setup.md) |
| 2026-08-02 | ent#89 | feat(templates): **a template's declared `schedules:` are materialized at agent creation**, for BOTH `github:` and `local:` sources. The abilities plugin ecosystem treats that block as an agent's design source of truth, but only `/trinity:sync` ever reconciled it — UI/API/MCP creation got none of it. New tolerant leaf `services/template_schedules.py` (total by contract; a raise would empty the catalog, enter creation's rollback fence, or fail-open the new compat check), surfaced by both builders, consumed by a normalized `_TemplateResolution.declared_schedules` carrier — deliberately NOT `template_data`, which is raw YAML on one path and `{}` on the other and whose truthiness gates credential-file generation. The `github:` half needs its own creation-time fetch (resolved PAT + parsed `@branch` ref, cache-bypassed, loud on failure): the catalog's copy uses the **global** platform PAT off the **default branch**, so a private-repo creator with a per-user token would have got zero schedules with no signal — the silent-ignore class this feature exists to close, one layer up. Three regressions #89 itself would have created are fixed with it: both GitHub catalog list paths **fenced** (bare comprehensions since ent#128 PR-A fenced only the local one), a name-match guard in `system_service.create_schedules` (it runs *after* creation, so it is now the second producer for the same agent), and `db.create_schedule`'s falsy return **checked** (it returns `None` on three paths and never raises). Compat gains **T-018** (structural only — A-002 owns cron) which **fails closed**, plus two live fail-open fixes: `c_p006`'s missing `isinstance` guard on a HARD check, and `_valid_cron` (A-002) which rejected `0 9 * * MON` and accepted `99 99 * * *` until it was pointed at the scheduler's own parser. | [template-processing.md](feature-flows/template-processing.md), [scheduling.md](feature-flows/scheduling.md), [agent-compatibility-validation.md](feature-flows/agent-compatibility-validation.md), [system-manifest.md](feature-flows/system-manifest.md) |
| 2026-08-02 | ent#109 | feat(git): **bind an agent to a GitHub repo you own** — the post-creation ownership retrofit, closing the last gate in ent#122's fresh-install flow. A tokenless ent#123 public-template agent (the default Cornelius) accumulates a knowledge base it cannot push anywhere, and the product's own message told the user to *create a new agent and import your data* — discarding the agent's identity, container, 180-day name reservation and history to keep a directory. `POST /{name}/git/bind-to-own-repo` creates a user-owned repo from the agent's **current workspace volume** (not its template — that is why it cannot reuse ent#93's copy step), repoints `origin` in place, persists the per-agent PAT and re-bakes the container env. Framed as a **rebind, not a fork verb**, so an already-writable agent is an ordinary rebind rather than a refusal — which is what makes AC #3 ('works for any agent') literally true *and* less code. Classification partitions on **`source_mode`**, the column `idx_git_config_repo_branch_unique` actually keys on, not on write credentials (an orthogonal column: a credential-less `source_mode=0` row would pass a credentials gate and be rebound *inside* the index); every other shape is refused **by name**. Concurrency is a **destination-scoped** lock — the real race is two *different* agents targeting one repo, which no per-agent lock serializes — **failing CLOSED** (503), unlike `agent:data_op:`, because a lost lock here means two repo creates and two concurrent recreates of one container. The commit point is a single **CAS** (`rebind_git_config`, predicate in its docstring); the loser is a compensating **restore of captured previous values, never `delete_git_config`** — ent#93 may delete because its row was INSERTed microseconds earlier, but on a *pre-existing* row that strips a live agent's binding and the next recreate brings it back with no repo (#843/#1439). The **PAT is persisted LAST** (early made `_agent_has_write_credentials` report already-writable, so the documented retry returned 409, and let a mid-window Push hit the OLD repo with the NEW token) but **before** the recreate (whose `pat_gate="per_agent_only"` would otherwise bake a repo-bound container with no token that startup.sh blackholes). The recreate is **mandatory**: startup.sh rewrites `origin` unconditionally from baked env, so a DB-only rebind is silently reverted — and it is **not** a re-provision (same volumes via the `volume_base_name` pin, so AC #2's S4 allowlist holds by construction). ent#93's destination triage is shared as `inspect_or_create_destination_repo()` (`created|empty|branches`) with reuse/refuse **policy per caller**, because the create path's reuse branch IS its template-tip SHA comparison. Both `no_write_credentials` surfaces retired (Invariant #13, source-guarded). Decision #17's drift predicate **cut**: its only drift-proof form calls `_apply_git_env_from_db` and turns PR 1's AST writer-set guard red. Convergence comes instead from an **explicit resumption branch** — review found that "idempotent retry supplies it" was false: after the CAS the row names the destination while `origin` still names the old repo, and both pre-flight gates read that skew as a refusal, so all four post-commit messages promised a retry that returned 409 (and the regression test written for exactly this passed only because its double derived the container's origin FROM the row). A row already naming the destination now relaxes both gates — safe because `origin` never selects what is pushed and the push carries no `--force`/`+` refspec. The recreate also clears name-keyed breaker state (#1560 — this is that helper's second production call site). Security: the user PAT is header-charset-validated at the model (h11 echoes an illegal header value, so a pasted trailing `\r` put the raw token in a 500 body and the platform log), with Pydantic's `input` stripped from every 422 so the guard closes the leak instead of relocating it. | [agent-repo-binding.md](feature-flows/agent-repo-binding.md), [github-sync.md](feature-flows/github-sync.md), [github-repo-initialization.md](feature-flows/github-repo-initialization.md), [mcp-git-tools.md](feature-flows/mcp-git-tools.md) |
| 2026-08-01 | #1932 | fix(whatsapp): **inbound media download** — Twilio 302s every attachment to `mms.twiliocdn.com`, which the SSRF allowlist refused, so `download_file` returned `None` for all inbound media since #463 (2026-04-23). The allowlist splits in two tiers so the fix widens only the gate that had the bug: `_TWILIO_MEDIA_SOURCE_HOST_SUFFIXES` (`*.twilio.com`) for the webhook parse gate + the credentialed hop 1, `_TWILIO_MEDIA_ALLOWED_HOST_SUFFIXES` (`+ *.twiliocdn.com`) for validated, unauthenticated redirect targets only — the tenant's AuthToken is never sent to a CDN host. `follow_redirects=False` stays with every hop re-validated (single follow → bounded budget); `s3-external-1.amazonaws.com` deliberately excluded (path-style ⇒ arbitrary buckets); transport size cap; no env override (a tenant-credential gate is not a platform-scope knob). First-ever `download_file` coverage, hop-count-asserted | [whatsapp-integration.md](feature-flows/whatsapp-integration.md) |
| 2026-08-01 | #1931 | fix(templates): **the shipped catalog stops advertising a demo fleet and a dead repo list**. A fresh install's Library showed 14 local templates — 11 of them the `dd-*` VC due-diligence demo — plus 6 GitHub repos last pushed Dec-2025/Jan-2026 that no install had ever overridden. Now **3** (`sage`/`scout`/`scribe`) and **0**. The `dd-*` fleet is `hidden: true` (#1513 mechanism — omitted from the listing, still creatable by id) and stays deployable as a set via `config/manifests/vc-due-diligence.yaml`, **promoted** from `docs/demos/` because a working manifest already existed and its system name is load-bearing: deployed names are `f"{manifest.name}-{short}"` and `dd-lead/CLAUDE.md` hardcodes its roster as `vc-due-diligence-dd-*`, so a "tidier" name deploys 11 healthy containers whose Deal Lead reaches nobody (now a red CI run, not a silent demo failure). `DEFAULT_GITHUB_TEMPLATE_REPOS = []` — a **browse** surface removed, never a **create** capability (`github:owner/repo` still resolves through the dynamic branch, proven by test rather than by reading), with the side-effect that `GET /api/templates` makes zero outbound GitHub calls on a cold cache. Library's GitHub section no longer *vanishes* at zero: a marketplace-first placeholder card (abilityai/abilities wizards → owner/repo CTA → role-branched curation hint), mutually exclusive with the page-level empty state by construction; Settings' matching dead end fixed at the destination. Every bundled directory must now **declare** `hidden:` (CI-enforced; runtime default deliberately unchanged, since flipping it turns a forgotten key into a *silent* absence). | [library-page.md](feature-flows/library-page.md), [template-processing.md](feature-flows/template-processing.md), [platform-settings.md](feature-flows/platform-settings.md), [system-manifest.md](feature-flows/system-manifest.md) |
| 2026-07-30 | ent#126 | feat: **install a system from a manifest via the UI** — `POST /api/systems/deploy` existed but was reachable only by curl/MCP. Home is a third stacked `#systems` section on the **Library** page, not a 7th NavBar entry. It shipped first as a `?tab=` strip on `Templates.vue`; ent#263 renamed that page to Library and deliberately chose stacked sections + jump anchors over tabs, so this conforms to the page's model rather than competing with it — Systems sits beside Agent Templates because both *install agents*, leaving Skills last as the kind that configures existing ones, and it is hidden outright below `creator` rather than shown-and-disabled; three `components/systems/*` over a new domain-scoped `stores/systems.js` (Invariant #6 — NOT `systemViews.js`: a System is a name-prefix group, a System View is a tag filter). Two read-only catalog endpoints (`GET /manifests`, `GET /manifests/{id}`, both `require_role("creator")`) declared above the parameterized routes for **two** Invariant-#4 collisions (`/{system_name}` AND `/{system_name}/manifest`), fail-soft per file, with layered path confinement (the regex does **not** reject `..` — dots are in its class) and open-once+`fstat`+`O_NOFOLLOW` reads because `config/manifests` is a host bind mount. The dry-run gains `permission_edges`/`schedules_preview`/`system_view_requested` from **pure resolvers shared with the writers** (`configure_permissions`/`create_schedules` now loop over them; only the backend knows the resolved `_N` names) — behaviour preservation pinned by characterization tests captured green BEFORE the refactor, since a resolver-vs-refactored-writer parity test is tautological and the ent#125 suite mocks both writers. `_preflight_template` now validates **merged** resources through the create path's own normalizers: a shipped bundled manifest carried `cpu: 1.0`, previewed `valid`, and failed 100% of its agents at 500 — that manifest (a broken duplicate of the live seed, same `acme` system name) is deleted, and a table-driven test over the real directory now guards the class. Store switches on `status`, never the HTTP code (`partial`/`invalid` are 200; `failed` is 500 **with the report as the body**, which a naive catch discards along with the `failed[]` list); result reads "agents created" not "success" (`status` excludes config/start failures) with `warnings[]` prominent; `prompt:`/enabled-schedule manifests need an **explicit acknowledgement**, not a banner; a timeout reports "outcome unknown" and offers no retry (cancelling does not cancel the serial server deploy). Plain textarea — the orphaned monaco `YamlEditor.vue` stays unrevived because prod CSP is `script-src 'self'` while dev allows `unsafe-eval`, so `npm run dev` cannot prove prod. | [system-manifest.md](feature-flows/system-manifest.md) |
| 2026-07-31 | #1919 | fix(ops): **fleet-restart lock hardened** (#1912 review follow-up). The per-iteration lease refresh became an ownership-checked pre-action gate (shared `redis_breaker_util.lock_token_matches`): **foreign** token ⇒ stop with partial-run honesty (`summary`/audit gain `processed` + `stopped_early="lease_lost_foreign"`; completed restarts stand); **absent** token (incl. EXPIRE→0 sliver) ⇒ SETNX re-acquire and continue (`lease_reacquired` audited); refresh Redis errors fail-open with one throttled warning. `list_all_agents_fast()` moved inside the `try/finally` (abnormal exit audited as `stopped_early="error"` + class name — distinguishable from an empty fleet); TTL 900→2100, sized above `skill_service._INJECT_LOCK_TTL_SECONDS` (1800) so one slow agent can no longer outlive the whole lease. +11 unit tests; sibling lock sites → #1920 | [agent-lifecycle.md](feature-flows/agent-lifecycle.md), [mobile-admin-pwa.md](feature-flows/mobile-admin-pwa.md) |
| 2026-07-31 | ent#263 | feat(library): **Templates page renamed to Library** — one surface for installable assets. `/library` (nav, route, `meta.title`) with an Agent Templates section (former page content; fetch migrated to the shared api client) and a new Skills fleet-browse section (`LibrarySkillsSection.vue` + fleet-scoped `stores/skillsLibrary.js` — separate from the KeepAlive-exposed `stores/skills.js` — + the shared `components/skills/` contract-chips seam consumed by both the Library and the per-agent Skills tab). `/templates` redirects (query+hash-preserving). Zero backend change; dead `env-template`/`refresh` doc+test rot swept. Flow renamed from `templates-page.md`. | [library-page.md](feature-flows/library-page.md) |
| 2026-08-13 | #1677 | fix(operator-queue): **platform-emitter budget** — the #1632 exemption scoped by *influence*, not caller location. The agent-influenceable `_alert_skill_not_found` (#1410; distinct unknown slash-commands defeated its per-command dedup at $0/turn) now routes through `operator_queue_service.create_bounded_alert`: a per-(agent, registered-type) pending-DEPTH budget (`OPERATOR_ALERT_MAX_PENDING_PER_TYPE`=5, DB-measured ⇒ Redis-independent; `_BUDGETED_ALERT_TYPES` frozenset; **fail-closed on every arm** — the FAILED execution rows stay the primary surface; the paired notification gated on the same bool; dedup→budget→creates ordering pinned; command truncated ~200 chars on every echo). At the cap: ONE cooldown-gated episode alert with deterministic reserved id `alert-budget-{agent}-{type}-b{bucket}` (DB on-conflict = cross-worker dedup; no `held`, no agent-controlled text). `count_pending_for_agent(+item_type)` query-only (no migration); `create_item` belts the derived `execution_id` COLUMN. AST caller-parity guard forces classification of every `create_operator_queue_item` call site (`_ALLOWED_CALLERS` = the living inventory; OSS-scoped, enterprise twin flagged). Closes the platform-emitter residual on the #1081 pull-default-ON gate list | [operating-room.md](feature-flows/operating-room.md) |
| 2026-08-01 | #1900 | fix(templates): **`local:` template ids are contained on the read path, and credential staging stops deriving a path from `name:`**. `GET /api/templates/{id:path}` joined the caller-controlled `<name>` onto the templates root unvalidated — `local:../x`, `local:/abs/x` and root-escaping symlinks disclosed any parseable `template.yaml` (incl. other tenants' uploads under `/data/deployed-templates`) to any authenticated role. New public `template_service.contained_template_dir` applies the create path's #950 barrier (name allowlist, then `resolve()` both sides + `is_relative_to`); rejects return `None` → a 404 byte-identical to an unknown template (#1759 non-disclosure). The AC#4 audit found a second, credential-bearing sink: `generate_credential_files` derived the `.mcp.json` directory from the template's untrusted `name:`, so an uploaded `name: ../../data/deployed-templates/<victim>` read another tenant's `.mcp.json` into the attacker's own agent — now fed the already-validated directory via the extracted `crud._resolve_local_template_dir` (the #1759 seam-agreement property, extended from two seams to three). | [template-processing.md](feature-flows/template-processing.md), [local-agent-deploy.md](feature-flows/local-agent-deploy.md) |
| 2026-07-31 | ent#261 | feat(ui): **Dashboard `/` type-to-filter across Timeline / Grid / List**. Hotkey-activated floating filter pill (guards: editable targets, modals, chords, IME, key-repeat) filtering agents live per keystroke via the store `visibleAgents` seam — split into pre-query `ownerFilteredAgents` (feeds ALL `convertAgentsToNodes` sites, incl. the 30s poll that previously rebuilt raw) ∘ query (slug + display label via `agentDisplayName`, #1642). Timeline joins by `:agents` prop switch — **owner filter now applies to timeline rows too** (deliberate, release-noted). Honest state: "X of Y match" count, pill visible whenever a query is applied, ONE chassis query-empty overlay with panes kept MOUNTED (`!filterActive` guards make the onboarding CTA unreachable under a query), list-panel N/M badge suppressed while the chassis query is active. Esc layering (input-scoped + gated doc backstop; tag-dropdown dismissal keeps the filter), Enter blur-and-keep, clickable header kbd hint (toggle). **Nothing persisted** — reload/unmount always unfiltered. Zero backend. No standalone flow doc — folded into the three dashboard flows | [dashboard-grid-view.md](feature-flows/dashboard-grid-view.md), [dashboard-timeline-view.md](feature-flows/dashboard-timeline-view.md), [dashboard-list-view.md](feature-flows/dashboard-list-view.md) |
| 2026-07-31 | ent#260 | feat(ui): **Dashboard List view replaces the Agents page**. `views/Agents.vue` deleted; its row list extracted into props-driven `components/AgentListPanel.vue` and mounted as the third dashboard mode (Timeline / Grid / **List**) through the existing VIEW_MODES machinery (persistence + degrade guard free). Rows read `tags`/`read_only_enabled` off the fleet payload — **both page N+1 mount loops deleted** (zero per-row HTTP, payload-truth read-only on stopped agents); toggles rewired to networkStore ({success,error} toasts; system rows adopt the grid's Run-toggle guard). Store-level `visibleAgents` computed = the named ent#261 seam (grid + list props; timeline via ent#261's own `:agents` switch); `fetchAgents` write-through keeps #1643 tab titles warm. Tag/owner filters migrated to the chassis controls (Clear-all clears both layers); name/status persist under NEW `trinity-dashboard-list-filter-*` keys; chassis-level Create Agent (all modes). `/agents` → query-preserving `/?view=list` redirect — one-shot, **non-persisting** (`setViewMode {persist:false}`), stripped after apply; NavBar entry removed (Dashboard inherits the agent-detail highlight). Zero backend changes, no new endpoints | [dashboard-list-view.md](feature-flows/dashboard-list-view.md), [dashboard-grid-view.md](feature-flows/dashboard-grid-view.md) |
| 2026-07-31 | ent#265 | feat(channels): **Telegram completion report-back** — a Telegram-triggered long/delegated task posts its terminal (✅/⚠️) back to the originating chat, threaded to the triggering message. Per-channel resolver dispatch (D10) over the ent#224 seam; **binding-agent identity** (`schedule_executions.source_channel_agent` — consent + bot token evaluate against the bot the user actually addressed, transitive A→B→C, NULL = legacy; also fixes Slack's delegated case); Telegram consent: group `telegram_group_configs.allow_proactive` (default allow, opt-out "Completion reports" toggle, human-only PUT arm) / DM consent-by-construction. Two channel-agnostic fixes: **D0** — inherited channel context is now persisted ON the child row at /task creation (the `run_async_task`→`execute_task` threading was dead — the shipped Slack delegated report was latent dead code) + a provenance guard on `parent_execution_id`; **D3** — the `apply_result` failure branch now spawns the report (AC#2). HTML pre-escape + post-conversion 4096 cap; effect-guarded at-most-once with the executing-agent pin (D9) | [channel-completion-report.md](feature-flows/channel-completion-report.md), [telegram-integration.md](feature-flows/telegram-integration.md), [task-completion-events.md](feature-flows/task-completion-events.md) |
| 2026-07-30 | ent#109 | fix(lifecycle): **one owner for git env across both container-rebuild paths**. `recreate_container_with_updated_config` seeds `env_vars` from the **old container** and re-derived only `GITHUB_PAT` — replaying whatever `GITHUB_REPO`/`GIT_SYNC_*` each container carried; the derivation lived only on the other path (`_apply_persisted_auth_env` → `recreate_missing_container`). Its one production caller is `start_agent_internal`, fired by nine config-drift predicates **and base-image drift at cold start**, so a base-image rebuild arms the replay fleet-wide. New `_apply_git_env_from_db(..., *, pat_gate)` owns both. The **PAT gate is a required per-call-site parameter, never a default**: `per_agent_only` (config drift) keeps #211's opt-in verbatim so a global-only platform PAT is never injected into a previously-tokenless container, `effective` (rebuild-from-nothing) uses the 2-tier resolver — a verbatim lift would have shared the broad gate, `configure_push_remote` would clear the push blackhole, and a tokenless agent (Cornelius) could push a private KB to the shared public upstream (the ent#162 class). An **AST guard pins the writer SET** — `{recreate_container_with_updated_config: per_agent_only, _apply_persisted_auth_env: effective}` exactly — so a *third* writer on any container-seeded path fails CI, not just a flip of the two known ones (ent#109's bug WAS two writers with one wrong). Plus **set-or-clear** over `_GIT_ENV_KEYS` (a deleted `agent_git_config` row pops the whole set — the per-agent PAT is a column *on* that row) and `GIT_SYNC_AUTO` as **DB flag OR baked env**, derive-only, because `crud.py`'s two creation writers genuinely disagree (`and not config.ephemeral` inside a swallowing `try/except` on the DB side only) and DB-only derivation would silently stop auto-push — with **no** write-back, since "baked true / DB 0" is also exactly an owner's `PUT .../git/auto-sync {enabled:false}` and rewriting it would erase that intent *and* let an `AuthorizedAgentByName` Start flip an `OwnedAgentByName` flag. Plus **correct, never introduce** on the config-drift path: repo-gating the block while per-agent-gating the PAT would hand startup.sh `GIT_SYNC_ENABLED=true` with no token for an agent bound via `POST /{agent}/git/initialize` on the global PAT, and the restart branch would scrub its `.git/config` credential and blackhole its push remote. Tests: `test_ent109_git_env_seam.py` (22), each behaviour proved to have teeth by mutation. | [agent-lifecycle.md](feature-flows/agent-lifecycle.md), [github-sync.md](feature-flows/github-sync.md), [git-sync-health.md](feature-flows/git-sync-health.md) |
| 2026-07-30 | #1813 | feat(canary): **fail loud when the collector is blind** — Phase 5 adds `H-01`, the harness's first *self*-check and its own `H-` (harness-health) id family: every other invariant means the observed system is broken, `H-01` means the **observer** is, and its violation invalidates every other green in that cycle. #1540 repointed the SQL collectors at the live engine but left the failure *shape* — zero rows is indistinguishable from a clean fleet, so a re-blinded collector reports all-clear (verified: a 2-agent fleet on a diverged backend yields `known_agents=∅`, `sources_unavailable=[]`, **zero violations**). Prior coverage was worse than none: L-03 fires only if an execution happens to hold a slot, and then blames a *ghost agent* — sending the on-call after a delete-cascade bug instead of a blind detector. Evidence is deliberately **non-circular**: new `Snapshot.docker_agent_names`, taken from the container **list** before any `exec_run` (never `zombie_counts`, which is keyed by exec success and silently thins on a degraded container) ∪ Redis slot keys (corroborating only — an idle fleet holds none). Reasons `roster_read_failed` / `roster_empty_contradicted` (critical) / `roster_empty_unverifiable` (major — the dead-smoke-detector chirp). Confirmation is on **elapsed wall-clock**, not "a second cycle": prod runs `--workers 2` and `canary_service` holds no leader lease, so both loops share `canary:h01:suspect_since` and a cycle-count rule would let worker B confirm worker A seconds later, collapsing the gate inside the teardown window it exists to ride out. Fail-loud throughout — an unreadable *or unwritable* marker fires **unconfirmed** rather than skipping. Scoped to the roster read ONLY (`terminal_rows`/`enabled_schedules`/`orphan_refs` are legitimately empty on a quiet fleet, so a general "any collector reads zero" rule would false-alarm on every idle install). Pure function over the `Snapshot` ⇒ dual-track by construction; a real-PG firing test reproduces the #1540 shape. No schema change, no migration, no endpoint, no flag | [architecture.md](architecture.md) |
| 2026-07-30 | #848 | feat(mcp): **inline email auth** — an external collaborator signs in from inside their MCP client with the existing 6-digit email code, no pre-minted API key and no web-UI visit, then uses the exposed playbooks of every agent shared with that address. Flag-gated `MCP_INLINE_AUTH_ENABLED`, **default OFF** (byte-identical behaviour when off, and the backend surface 404s rather than advertising itself). New keyless `anonymous` session tier + `request_login`/`verify_login` (`tools/auth.ts`), the connector tools taught to serve a key-bound **or** email-verified caller, and a backend internal surface `/api/internal/mcp-auth/{request,verify,playbooks,chat}`. **Session, not a minted key**: an email-verified session holds no credential, so the MCP server relays over the internal surface and the backend re-gates **every** data call on `db.email_has_agent_access(agent, email)` — the internal secret authenticates the caller, never the action (`assert_email_may_reach_agent`). The `agent` argument is a **selector among already-authorized agents**, not a grant. `scope` stays `"anonymous"` after login so an inline session can never satisfy `operatorOnly`. Prereq hardening, independently valuable: the operator tool-visibility gate became an explicit `OPERATOR_SCOPES` **allow-list** — the old `!== "connector"` deny-check admitted every scope it had not heard of, and FastMCP skips `canAccess` filtering *entirely* for a falsy auth context, so a null-auth session would have been handed every registered tool. Enumeration-safe `request_login`: one constant 202 on every path with **no audit row**, all branch-dependent work deferred to `BackgroundTasks` (a committing INSERT on the known branch alone measured ~1.9×), `_email_is_known` fail-closed, rate limits account-scoped never per-IP (`client_ip` is always the MCP server — a per-IP bucket is the #591 fleet-wide DoS). Idempotency scope folds the verified email in, closing a cross-user response-snapshot disclosure between two verified users of one shared agent, reachable by accident because MCP clients derive deterministic keys from call args. The advertised tool list is identical before and after login — login flips *behaviour*, not *visibility*, since `toolsListChanged` re-filters live sessions and the #846 reconciler fires it every ~20s | [mcp-connector.md](feature-flows/mcp-connector.md) |
| 2026-07-30 | #1880 | fix(canary): **every invariant is now alertable**. Five registry invariants (E-03/E-04/E-06/G-03/G-04) shipped with no entry in the per-invariant alert surfaces, so a green→red page rendered `🚨 canary G-04 G-04 (critical): G-04 fired 1 violation(s).` — the **critical credential-leak check** with no name, no evidence and the id stuttered. There are **four** id-keyed surfaces in `services/canary_alerts.py`, not the two the issue named (`_INVARIANT_NAMES`, `_INVARIANT_RUNBOOKS`, and the branch chains in `_render_message`/`_render_forensic`) — and the two it missed carry the per-row evidence. Mechanism, not diligence: #882 populated all four, #1497 and #1472 populated none. Fix = all four surfaces × five ids + a bidirectional AST parity guard in **`tests/unit/`** (the 3,726-line canary suite at `tests/` root runs in *no* CI workflow, so a guard beside it would never go red). Names come from each invariant module's docstring title, **not** the catalog — catalog `E-06` is the unimplemented #129 check while registry `E-06` is "no overdue `next_run_at`" (#1472). Both render fallbacks kept state-free and pinned by a negative test: they are why an un-rendered invariant cannot echo `observed_state`, and a parity gate pressures toward a generic dumper. Also: `_mrkdwn_safe()` at the render boundary (56 sites) so Slack link/mention injection is structural rather than transitive across ~8 sanitize-at-write paths, and a `NULL` `agent_name` no longer raises into `canary_service`'s swallowing `except` — which silently dropped the alert while advancing the green→red cursor. No schema/endpoint/config change | [architecture.md](architecture.md) |
| 2026-07-30 | ent#128 | feat(templates): **the credential declaration standard**. `credentials:` is FROZEN names-only; the sibling `credential_setup:` enriches it per variable (title/description/required/secret/format/setup_url/default) and `normalize_credential_requirements(data, *, source_trust)` emits base-set-plus-overlay records as `credential_requirements`. A sibling key because enriching `env_file` in place is `TypeError: unhashable type: 'dict'` inside an older Trinity *while it writes the agent's `.env`* — structurally invisible instead, so no floor version. Joined BY NAME, so `credential_setup:` can only DECORATE and the pair cannot drift; an entry naming nothing is a named error and is dropped. Honest limit: for an external template that error is LOGGED, not surfaced (`credential_errors` has zero UI/MCP consumers). **Two HARD compatibility gates change verdicts**: K-001 read `.env.example` uppercase-only while the substitution engines impose no charset (`${my_var}` IS substituted), and K-002 compared `${VAR}`s against `set(credentials.keys())` — the SECTION names — so the documented structured form failed while `${env_file}` passed. And a HARD gate could **go dark**: `run_static` caught `Exception`→`skipped` while `_counts` counted only `fail`, so 4 lines of untrusted YAML dropped `hard_count` 1→0 and flipped `overall_status` to `compatible` — with `c_k002` delegating to `c_t015`, one raise took both gates. Now fail-CLOSED in three layers. Also the four deferred defects: the `crud.py` reach-through that cost `runtime:`+`shared_folders:` too, the same crash on the agent image's `/api/template/info` (with the BEHAVIOURAL parity guard no test gave that file before), MCP-server precedence flipped so all three surfaces agree, and the dead badge now derived with `platform_injected` EXCLUDED (it was correct on 1 of 7 real repos). Contract: `docs/schemas/trinity-agent-credentials.schema.json`, with `config_files` enumerated-and-deprecated rather than undocumented — a contract that answers VALID to `/etc/cron.d/pwn` blesses a traversal. | [template-processing.md](feature-flows/template-processing.md), [agent-compatibility-validation.md](feature-flows/agent-compatibility-validation.md) |
| 2026-07-29 | #1871 | fix(infra): **container logs are bounded**. Docker's `json-file` driver has no default `max-size`/`max-file`, so every container log grew forever under `/var/lib/docker/containers/` — silently, until the Docker data root hit 100%, dockerd could no longer parse its own logs, and the fleet wedged at once (2026-07-27). Two halves, because compose cannot reach agents: an `x-logging` anchor on all 9 services in **both** compose files (`CONTAINER_LOG_*`), and `AGENT_LOG_CONFIG` in `capabilities.py` for the SDK-created agent containers (`AGENT_LOG_*`), threaded into the three create sites beside `AGENT_TMPFS_MOUNT` — one of which is the shared tail of both retrofit paths. Validation is fail-safe in **both** directions: malformed *and* out-of-range (>1g, >10 files) fall back to the bounded default, because a well-formed `1000g` passes a format-only check while effectively removing the cap — the exact failure the constant prevents; an explicitly-set rejection WARNs (#1039 inert-by-obscurity). Creation-time, so existing agents adopt on **recreate**, not restart. Vector is unaffected — rotation is lossless for live streaming (verified zero-gap across ~10 rotations on the `docker_logs`, `file` and daemon-follow paths); only post-hoc `docker logs` history shortens, and the platform's log endpoint defaults to `tail=100`. A CI guard (`test_1871_log_config_parity.py`, AST-based) fails when a **new** durable-container create site ships uncapped — the `learnings.md` "the create path is never one call site" class. | [container-capabilities.md](feature-flows/container-capabilities.md), [vector-logging.md](feature-flows/vector-logging.md), [agent-lifecycle.md](feature-flows/agent-lifecycle.md) |
| 2026-07-28 | #1804 | fix(execution): **the CAS-won terminal owns the paired activity close**. Recovery paths wrote the execution terminal and left `agent_activities` at `started` — the Timeline rendered the agent as still working for up to 2h, then the 120-min backstop closed it with a fabricated `duration_ms = now − started_at` (a 15-min run permanently recorded as a ~120-min failure). Third appearance of the #45/#767 class, both of which patched a single producer. Now one owner — `activity_service.close_execution_activity` (+ a sync spawn wrapper for the pull sink), reusing `activity_state_for_terminal` (#1332) and keeping the WS broadcast — called by **every** CAS-won terminal writer: `_write_terminal_and_gate` (its missing lost-CAS branch), `apply_result`, both backend-shutdown `CancelledError` handlers (which emit no #1578 event — how they hid), watchdog + startup `_recover_execution` (the latter's CAS bool was **discarded**), the two bulk sweeps, the lease reaper (park→FAILED, re-queue→CANCELLED), the pull sink, and terminate. The close is itself a **CAS** (`ActivityCloseOutcome` tri-state) whose lattice *mirrors* the execution predicate — an authoritative COMPLETED may upgrade a provisional FAILED (the #1083 late-SUCCESS path), nothing overwrites an authoritative close — with the lookup widened to agree (a narrower lookup makes the fix inert). Bulk sweeps close set-wise in one transaction with no per-row WS, off the `collect_failed` rows #1714 already gathers. The 120-min sweep is demoted to a backstop and moved after the slot reaper; `CleanupReport.activities_closed_on_recovery` is the signal. Static parity guard anchored on **terminal writes**, not on event emission. No schema change, no migration, no flag | [activity-stream.md](feature-flows/activity-stream.md), [task-execution-service.md](feature-flows/task-execution-service.md), [dashboard-timeline-view.md](feature-flows/dashboard-timeline-view.md) |
| 2026-07-28 | #1816 | fix(system-agent): **`trinity-system` now adopts a rebuilt base image**. `ensure_deployed` returned `action: none` the instant the container reported `running` — no drift predicate ever ran — and with `restart_policy: unless-stopped` plus a canonical upgrade path (`build-base-image.sh` → `start.sh`) that never touches agent containers, the orchestrator was the **most-stale agent in every fleet**. Three parts. (1) **Convergence**: `_create_system_agent` never wrote `TRINITY_AGENT_AUTH_TOKEN` and never labelled `trinity.full-capabilities`, so two of the eight config predicates were **permanently** false — and because a config recreate resolves the image from the container's own `Config.Image` **tag**, every config recreate is also an image adoption, i.e. the first `POST .../start` after any fresh provision replaced a running orchestrator. Both converged, pinned by an **AST-derived** test that builds the fixture from creation's own `env_vars`/`labels` dicts. (2) **Cold-boundary adoption**: the stopped branch delegates to `start_agent_internal` (inheriting the #1809 image gate, #1560 clear-before-recreate, the 409/NotFound race hardening and the handle re-lookup) instead of a bare `container_start`; `/restart` too. The running branch is **read-only** — `check_base_image_matches` split into a 3-state `check_base_image_state` (+ unchanged boolean wrapper) so staleness can be *reported* without being acted on, feeding `base_image_state` on `/status` and an edge-triggered `base-image-stale-` operator alarm on `stale` **only** (never `unknown` — a fail-open probe must not manufacture an alert). (3) **AC2 as a structural gate**: `is_system AND was_already_running` skips the whole `needs_recreation` block and returns `recreate_deferred="system_agent_running"`, so a tenth predicate cannot reopen the hole. Also fixes `unless-stopped` silently vanishing on **every** agent recreate (the extracted `HostConfig` was never read) and makes `check_full_capabilities_match` system-aware — pinning the label alone would never converge on an `agent_full_capabilities=false` install (recreate on every start, forever). | [internal-system-agent.md](feature-flows/internal-system-agent.md), [agent-lifecycle.md](feature-flows/agent-lifecycle.md) |
| 2026-07-28 | ent#128 | fix(templates): a malformed `credentials:` block must not empty the catalog. One template whose `credentials:` (or `credentials.mcp_servers`) was a list/string/**null** raised an uncaught `AttributeError` out of `_build_local_template` → `GET /api/templates` **500, zero templates listed**; the `github:` builder had no `isinstance` guard at all on untrusted repo metadata; and deeply-nested YAML escaped the `yaml.YAMLError` handler as `RecursionError`. Four tolerant readers in `template_service.py` (`credential_shape_errors` / `credential_mcp_server_names` / `credential_env_file_names` / `CredentialDeclarationError`): **read paths never raise** — degrade, attach `credential_errors` to the entry, one WARNING per bad template, and it **still lists** (plus a per-template fence in `get_local_templates`); the **write path fails loud** — 400 `INVALID_CREDENTIAL_DECLARATION`, closing the silent `.env` corruption where `env_file: "OPENAI_API_KEY"` (list dash forgotten) was iterated character-by-character into 15 single-letter vars with no error at all. `config_files[].path` absolute/`..` rejected at the boundary AND re-checked at the write sink (`crud._safe_cred_file_path` → 400 `INVALID_CREDENTIAL_FILE_PATH`), closing an arbitrary-file-write primitive reachable from `deploy_local_agent_logic`. Absent/null/`{}` stays a valid zero-credential contract. PR-A of ent#128; the enriched per-variable declaration is PR-B. | [template-processing.md](feature-flows/template-processing.md) |
| 2026-07-27 | #1759 | fix(agents): an **invalid `local:` template now fails loudly**, and the `local:default` id resolves again. Companion to #1793 (PR #1803), which closed the *absent*-template half with 404 `UNKNOWN_LOCAL_TEMPLATE`; the two were filed and built in parallel against the same line and #1759 conceded that contract at merge. What remained unfixed: an empty/non-mapping/unparseable `template.yaml` still reached the identical blank-agent-at-200 outcome through the broad `except Exception: logger.warning` — now **400 `LOCAL_TEMPLATE_INVALID`**, same pre-side-effect band, with the broad `except` narrowed to field-level degradation only (and its warning now naming the template + agent). The curated root gains a repo-relative fallback (hand-rolled — importing `template_service` would be satisfied by the #1484 harness MagicMock; guarded against `IndexError` in the container layout, where it is computed at import time) so the gate is not inert wherever the suite runs; **both** seams take it, and the `/template` bind base becomes `os.getenv(...) or _default_host_templates_base()` (an empty `HOST_TEMPLATES_PATH` made `Path("")/name` an empty NAMED VOLUME — the same bug class one seam over). Ships `config/agent-templates/default/` (hidden, deliberately no `resources:` — it would neutralise ent#125's `cpu: "3"` failure vector), **fixing a live regression #1793 introduced**: `local:default` is named 46× in `tests/test_systems.py` and by `export_manifest`, and with no such template on disk the new 404 broke every one of those deploys — invisible to CI, which collects `tests/unit/` only. `export_manifest`'s unreachable `local:business-assistant` `.get` default fixed with `or` (pre-existing broken round-trip for Blank Agents, not a regression). Manifest deploys surface the reject per agent via ent#125 `failed[]`. Together these close the follow-up left open by #843. | [template-processing.md](feature-flows/template-processing.md), [system-manifest.md](feature-flows/system-manifest.md) |
| 2026-07-18 | #1296 | feat: **agent self-reminders** — durable one-shot deferred self-trigger (the time-deferred sibling of loops). New `agent_reminders` table (5-track migration, `AGENT_REFS` CASCADE); the standalone scheduler arms a `DateTrigger` per pending row (`_reconcile_reminders` at boot + 60s sync-loop own-try + full-reload), on fire creates the `schedule_executions` row itself (real id, `__manual__`) and dispatches `triggered_by="reminder"` through the standard `capacity_manager` path. At-least-once delivery (`firing` state + `fire_attempts` cap + stale-firing reclaim; TimeoutException=assume-dispatched-no-force-FAILED, clean-failure=status-guarded FAILED+retry). Self-only MCP `set/list/cancel_reminder` (connector rejected, off the ghost fence); create-idempotency over raw input (terminal rows excluded from replay); bounds (pending 25 / durable daily 100 / min-60s / max-30d / timeout-clamp-to-cap); retention sweep via the #1644 guard. Three trigger constants wired (first-class "Reminders" bucket). | [agent-self-reminders.md](feature-flows/agent-self-reminders.md) |
| 2026-07-17 | #1632 | fix(operator-queue): ingestion **rate/depth/size caps** at the agent-authored sync seam (blocking pull default-ON). Per-agent DB-measured pending-**DEPTH** cap (primary, Redis-independent) + per-agent/fleet **RATE** limit (`rate_limiter.check`, fail-open) + total field-hygiene clamp (truncate-with-marker, `created_at` normalized, inside the #1525 try/except) + **reserved-id guard** (an agent can't pre-suppress its own flood alarm / the #1402 poison alert) + oversized-file/scan bounds. Sync loop **leader-locked** (`opqueue:leader`, mirror monitoring #1464). One un-guessable `queue-flood-*` alert per episode. `validation_service` notify → **direct DB create** (exempt-by-construction + latent-bug fix). Generous DB-sink belt. Layers on the #1631 `id`/`request_id` split. | [operating-room.md](feature-flows/operating-room.md) |
| 2026-07-17 | #1310 | refactor(auth): **INV-8 imperative auth-guard family** — consolidate duplicated inline auth wiring behind five leaf helpers in `dependencies.py` (`assert_admin`, `assert_agent_access`, `assert_agent_owner`, `assert_owns_or_admin`, `assert_owns`), all raising 403 and access-first (self-uniform per #186); the agent-name helpers run `_enforce_connector_scope` first. ~30 router sites migrated (admin/access/owner/Shape-F/strict-self), preserving exact status+detail+admitted-principal set (proven by `test_1310_auth_consolidation`). Static AST drift guard `test_1310_auth_wiring` (`# noqa: inv8` for deferred `slack.py`). `_narrow_to_agent` relocated to the service layer. `public.py` strict-self stays no-admin (`assert_owns`); `sessions._session_or_404` / `nevermined._require_*` / `reports.get_report` kept as intentional-404 exceptions | [role-model.md](feature-flows/role-model.md) |
| 2026-07-18 | #1483 | refactor(routers): split `routers/chat.py` (2,756→988 ln) — Invariant #1 enforcement for chat. Business logic moved into four new leaf services: `chat_execution_service` (the `/chat` `run_chat_turn` applier decomposed per §3a + the `/task` dispatch orchestration decomposed + `terminate_execution`; **transitional** sync-chat applier, still delegates `/task` to `task_execution_service.execute_task` — never a 2nd applier), `dispatch_admission_service` (idempotency+audit+`/chat` capacity acquire), `chat_persistence_service` (#1444 persistence), `chat_signals` (`ChatDispatchError`/`ChatAdmissionReplay` domain signals). Services are HTTP-free; the thin router maps signals 1:1. **No behavior change** — OpenAPI byte-identical (372 paths), every function now ≤ CC 20 / ≤ 150 ln, route order preserved (Invariant #4 guard). #1444 fail-loud + IDOR, #1578 reserved-event tag, #914 queued-202, the upload-502 no-idempotency-fail quirk, and the full SUB-003 switch matrix preserved byte-for-byte. `run_chat_turn`→`execute_task` convergence is a tracked follow-up | [authenticated-chat-tab.md](feature-flows/authenticated-chat-tab.md), [task-execution-service.md](feature-flows/task-execution-service.md) |
| 2026-07-18 | #1164 | ci(security): **commit-time secret scan** — `.github/workflows/secret-scan.yml` runs the MIT gitleaks CLI binary (version + sha256-pinned; not the org-licensed `gitleaks-action`) on **every** PR (no path filter / no label gate — #878) over the PR/push **commit range** (`git merge-base`, `--redact=100`, `--exit-code 2` tri-branch fail-closed). `.gitleaks.toml`: `[extend] useDefault=true` + custom `trinity-resend-api-key` rule (bare `re_` key, entropy 3.8 — the sole `re_` coverage; verified default set misses it) + blanket allowlists (`tests/**`, historical doc trees, sanctioned placeholders). Closes the #1158 re-land gap. Honest limit: split/XOR-obfuscated secrets evade regex (the #1158 base85 halves are verified-undetectable, so `.gitleaksignore` carries no fingerprint). **Detection-only until a repo admin makes `secret-scan` a required check** (follow-up). No runtime flow — CI-only. | [architecture.md](architecture.md) (Container Security), requirements/security.md §20.8 |
| 2026-07-18 | #1540 | fix(canary): un-blind the CANARY-001 harness on **PostgreSQL** — repoint the six remaining SQL-tier collectors (`_collect_known_agents`, `_collect_executions`, `_collect_terminal_executions`, `_collect_terminal_rows`, `_collect_enabled_schedules`, `_collect_orphan_refs`) off raw sqlite3 (`db.connection`, which ignores a non-SQLite `DATABASE_URL`) onto the `get_engine()`/`DATABASE_URL` seam, finishing the #1450 collector-wide migration. Pre-fix, `_collect_known_agents` returned zero rows on PG → `snap.agents` empty → the whole per-agent loop (S-01/E-05/B-02/E-04/G-04/B-01) + fleet-wide reads (E-02/E-03/G-03/E-06/L-03) went vacuously green. `PRAGMA table_info` → portable `sqlalchemy.inspect()`; orphan-scan PK from the **static** `db/tables.py` Table (composite-PK `agent_tags` → synthetic row_id, never reflection); `sqlite.*` `sources_unavailable` labels kept verbatim (L-03/E-02 skip-contract prefix). No schema change (read-path repoint); verified against a real `postgres:16-alpine`. Closes pull-migration gap G3. | [architecture.md](architecture.md) |
| 2026-07-18 | #1484 | refactor(agents): **decompose `create_agent_internal`** (CC 187→10, 945→87 SLOC) into a thin orchestrator over fenced private phase-helpers (each CC<20 / <100 SLOC) reading a single orchestrator-populated `_RollbackHandles` dataclass — pure move, byte-identical, callers/signature unchanged. The `if docker_client: try/except/else` + CC-trivial gates stay inline; the github+fork phase stays OUTSIDE the try (FORK_* 4xx not flattened to 500); network hard-coded in `_create_agent_container` (AC#5). Characterization-tests-first: new `tests/unit/test_1484_create_agent_characterization.py` (40 tests) pins the contract; module split to `creation_phases.py` deferred to #1028 | [agent-lifecycle.md](feature-flows/agent-lifecycle.md) |
| 2026-07-17 | #1449 | fix(retention): terminal `backlog_metadata` **PII scrub** — new `db.scrub_terminal_backlog_metadata` NULLs the drain-replay blob (`user_message`/`user_email`/`system_prompt`) on authoritative-terminal rows (`success`/`cancelled`/`skipped`) via a chunked SELECT-ids-then-UPDATE, wired as an **unconditional** sub-sweep in `cleanup_service._sweep_retention_772` (security invariant, not age-gated, no ops key — dodges the #1638 floor-by-seed trap; count-only logging; folded into the WAL-checkpoint sum). **FAILED excluded** (resurrectable to SUCCESS via late CAS). No schema change; nothing reads terminal `backlog_metadata` (drain is queued-only, callbacks read the POST payload, canary E-04/G-04 queued-scoped). Sibling #1444 callback/pull-path chat persistence deferred to #1081. | [cleanup-service.md](feature-flows/cleanup-service.md) |
| 2026-07-17 | #1616 | fix(ssh): enforce ephemeral-key TTL on the **filesystem** — wire the previously-callerless `SshService.cleanup_expired_credentials()` into the 5-min `cleanup_service` loop (`_sweep_expired_ssh_credentials`; expired keys were removed from Redis metadata only and still granted login on a preserved volume). The sweep is `expires_at`-driven with a store-side Redis grace (600 s > the 300 s cadence) so every expired key is observed before Redis forgets it — a naive `ttl in [0,60]` window would have missed ~80% across the cadence. `inject_ssh_key`: single atomic `sh -c`, key passed via exec **environment** (no interpolation) + `skip_if_present` append-if-absent — kills the double-add and the shlex/`sh -c` injection class. `remove_ssh_key`: exact awk `$NF` last-field match via `ENVIRON` (no substring over-delete, no metachar escaping). Recreate-wipe symptom = separate unidentified mechanism; no start-time re-inject hook added | [ssh-access.md](feature-flows/ssh-access.md) |
| 2026-07-28 | ent#206 | feat(eval): **behavioral-eval referee surface + completion relabel** — `agent_evaluations` keeps the `quality` axis apart from the clean-exit `completion` axis, behind a write fence the graded agent cannot cross (`require_admin` + `reject_agent_principal`, since an agent key inherits its owner's role); `agent_reports` (#918) was rejected as the surface because its create is self-gated. Read stays access-scoped so an agent can see its own score. Relabels "Success rate" → "Completion" on Overview/#1115/EXEC-022 (additive — `success_rate` field unchanged). OSS-core per the strategy gate §10; the Tier-0 grader that populates `quality` is the next child | [agent-evaluations.md](feature-flows/agent-evaluations.md) |
| 2026-07-28 | #1535, #1538 | feat(reports): **make reporting discoverable and readable back** (epic #1534, children 1–2) — a "Publishing Reports" block in `PLATFORM_INSTRUCTIONS` (call, when to reach for it, payload shape per `display_hint`, CI-pinned to the MCP enum + renderer keys) so reporting is a fleet-wide default instead of a per-template opt-in; plus `list_reports`/`get_report` MCP tools over the existing access-controlled REST endpoints, narrowing an agent key's reads to `{self} ∪ permitted` (the backend scopes to the key's OWNER) and preserving the 404-not-403 id privacy on a denial | [agent-reports.md](feature-flows/agent-reports.md) |
| 2026-07-17 | #1578 | feat(events): **system-emitted task completion events** — the backend emits `agent.task.completed`/`agent.task.failed` at every CAS-won execution terminal (apply_result ×2, `_write_terminal_and_gate` timeout/budget/crash class, the #1083 lease-reaper, the pull sink), delivered over EVT-001 subscription dispatch so a subscribed orchestrator is woken instead of polling. Matching-sub gated (empty ⇒ no row, no dispatch); shared `event_dispatch_service` helper (dispatch primitives extracted out of the router, Invariant #1); reserved `agent.task.*` namespace with 3-layer loop safety (reject reserved emit + block reserved self-sub on create/update + recursion-break via `triggered_by="event"`); best-effort delivery to a running subscriber (honest — a stopped subscriber's wake is dropped, the row persists); fail-open. Additive/inert — no schema change, no migration, no config | [task-completion-events.md](feature-flows/task-completion-events.md), [agent-event-subscriptions.md](feature-flows/agent-event-subscriptions.md), [task-execution-service.md](feature-flows/task-execution-service.md) |
| 2026-07-17 | #1593 | fix(operator-intake): surface delivery failure — success gated on true 2xx (a 3xx/4xx/5xx **response** logs at WARNING with status + the Worker's coded `error`, whitelisted to snake_case so no PII; a connect/timeout that **raises** logs at INFO), replacing the `debug` line that hid the 2026-06-22→07-13 endpoint outage for ~3 weeks. `_safe_error_detail` hardened (broad except + `isinstance(dict)`) so a non-JSON/non-dict body can't re-hide the WARNING; marker/retry semantics unchanged (observability only) | [first-time-setup.md](feature-flows/first-time-setup.md) |
| 2026-07-12 | trinity-enterprise#69 | feat: **Ephemeral "ghost" agents** — budgeted (`max_executions`/TTL, always-stamped expiry), volume-less, hard-discarded (crash-convergent 6-step primitive, no soft-delete/name reservation); entitlement-gated creation + atomic per-owner quota + spawn rate limit; budget gate at the TOP of `CapacityManager.acquire` (410 Gone) + `apply_result` terminal hook; GC sweep (DB pass + Docker-as-truth orphan pass w/ newborn grace); ghost-key containment fence; Part 2 spawn provenance + auto-granted parent→child permission edge + child-scoped start/stop/delete + human-only share/permissions/rename/credentials | [ephemeral-agents.md](feature-flows/ephemeral-agents.md) |
| 2026-07-07 | #1077 | feat(canary): Phase 4 queued-row integrity invariants (Phase B) — E-04 (queued rows have `queued_at` + non-NULL, JSON-parseable `backlog_metadata`, guarding `backlog_service.drain_next` against `JSONDecodeError`) + G-04 (no raw credential prefixes in `backlog_metadata`, secret-scan) over the queued-row metadata `_collect_executions` now captures (`AgentSnapshot.queued_meta`), scoped strictly to `status='queued'` (#1449-safe). Read-only, registry-driven, no endpoint/service/DB change. **Credential-safe:** violations report a predicate reason / matched pattern name only, never the raw metadata. Stacked on #1450 | [architecture.md](architecture.md) |
| 2026-07-07 | #1077 | feat(canary): Phase 4 execution-row integrity invariants — E-03 (terminal rows have `completed_at`; `completed_at`-only predicate, dropping the catalog's `duration_ms` clause that false-fires on queue-terminated rows) + G-03 (`started_at ≤ completed_at`, ~1s tolerance, UTC-aware parse) over a shared `_collect_terminal_rows` collector (windowed on `started_at`, `LIMIT 5000`). Read-only, registry-driven, no endpoint/service/DB change. E-04/G-04 gated on #1450 | [architecture.md](architecture.md) |
| 2026-07-07 | #1500 | fix(ui): Tasks tab joins `FULLSCREEN_TABS` — task list flex-fills the viewport (old `max-h-96` cap becomes the `min-h-96` floor), panel root is the short-viewport fallback scroller; e2e fill/parity spec + `.first()` repair of the #954 spec broken by #1114's measuring mirror | [tasks-tab.md](feature-flows/tasks-tab.md) |
| 2026-07-07 | trinity-enterprise#107 | feat: auto-seed a default **Cornelius** second-brain agent on fresh install with the Brain Orb enabled — public `github:Abilityai/cornelius` template, anonymous source-mode clone on the ent#123 tokenless path (pre-generated seed graph so the orb renders immediately); first-run-only (durable `cornelius_seeded` flag) + fresh-install-scoped (`count_non_system_agents()`) `ensure_seeded()`; setup-completion BackgroundTask + `main.py` lifespan safety-net. Originally a vendored `local:cornelius` bundle; source-seeded in #1656 after vendoring drift caused #1646 + #1656 | [cornelius-default-agent.md](feature-flows/cornelius-default-agent.md) |
| 2026-07-06 | #1450 | fix(canary): B-01 queue-status coherence — Side B (queued id-count) reads via the SAME `get_engine()`/`DATABASE_URL` seam as the accessor (backend-consistent on Postgres, not raw-sqlite vs engine; #300/#1093 gap), dedicated `queued_ids_via_engine` field leaves `queued_exec_ids` for B-02/E-02; collector does one confirm-re-read so a transient enqueue/drain race self-heals while a persistent drift still fires; engine-read failure → B-01 skip. Production residue of #1446 | [architecture.md](architecture.md) |
| 2026-07-06 | #1489 | fix(frontend): plumb `VITE_BUG_REPORTING_ENABLED` / `VITE_BUG_INTAKE_URL` as prod build args (`ARG`+`ENV` in `Dockerfile.prod` before `npm run build`, `${VAR:-default}` compose `build.args`, `.env.example` docs) so the #1116 disable/repoint knobs actually reach the shipped image — Vite inlines `import.meta.env` at build time. compose==ARG==code default; disable works end-to-end, repoint still CSP-blocked (deferred #1485). Umbrella #1485 | [in-app-bug-reporting.md](feature-flows/in-app-bug-reporting.md) |
| 2026-07-06 | #1485 | fix(config): forward operator-intake / dispatch-breaker / public-access levers through **both** compose files (docs the #1039/#1067 packaging-gap class); honor cross-tool `DO_NOT_TRACK` convention in `config.py` (#1486/#1487/#1488) | [dispatch-circuit-breaker.md](feature-flows/dispatch-circuit-breaker.md) |
| 2026-07-06 | #1474 | fix: scheduler timestamps now serialize as UTC `Z` (vendored `src/scheduler/utils.py`; write+read atomic — naive-UTC parse preserves duration math); backend read boundaries + 5 execution panels normalize historical naive rows so non-UTC browsers stop showing shifted relative times | [scheduler-service.md](feature-flows/scheduler-service.md) |
| 2026-07-04 | #903 | fix(slack): thread-scoped session + per-speaker attribution + sender-filtered memory | [slack-channel-routing.md](feature-flows/slack-channel-routing.md) |
| 2026-07-04 | #1445 | fix(webhooks): gate schedule/webhook **creation** on a live owning agent (`is_agent_live` → 404; access-check-first so non-owners see a uniform 403) so a webhook token always resolves to a schedule of a live agent — closes the orphan-schedule class that made valid tokens 404 after #1423's soft-delete-aware token-lookup INNER JOIN | [webhook-triggers.md](feature-flows/webhook-triggers.md) |
| 2026-07-06 | trinity-enterprise#47 | feat(ui): Dashboard **Grid view** — third mode (Grid/Graph/Timeline, not default): magnetic tile canvas on an unbounded pan/zoom lattice; five-zone 384×216 tiles (adaptive chips, Activity·14d + Context·7d charts, Run/Auto toggles); drag with socket preview + swap, tidy/reset, keyboard reorder; skeleton-first render, viewport-gated cached analytics hydration, visibility-aware batch polling. No new backend endpoints | [dashboard-grid-view.md](feature-flows/dashboard-grid-view.md) |
| 2026-07-04 | #1018 | fix(nevermined): settlement-ordering — honest `success_unsettled` on settle-fail (was lying `"success"`); `Idempotency-Key` on the paid boundary keyed on `(payment-signature ∥ message)` with in-flight-409 / settled-replay / unsettled-re-drive-converge; `/retry-settlement` stub → honest 501. Tier 2 durable retry split to a follow-up | [nevermined-payments.md](feature-flows/nevermined-payments.md), [effect-idempotency.md](feature-flows/effect-idempotency.md) |
| 2026-07-04 | #186 | fix(security): close user & agent enumeration oracles — uniform 404 across the agent-access dependency family + router sweep + Tier-4 GETs; identical email-request body/status/timing; MCP `checkAgentAccess` uniform reason + owner-username removal (pentest 3.3.3) | [email-authentication.md](feature-flows/email-authentication.md), [agent-permissions.md](feature-flows/agent-permissions.md) |
| 2026-07-04 | #1444 | fix(chat): fail-loud + owner-gated `/task` chat-session persistence (no silent swallow, IDOR fix, SUCCESS-guarded) + fast unit regression guard | [authenticated-chat-tab.md](feature-flows/authenticated-chat-tab.md) |
| 2026-07-04 | #1446 | fix(canary): stop B-01 queue-status coherence false-firing under full-suite load — canary tests own the `database` dep (temp-DB stub via `get_engine`) + first end-to-end B-01 coverage (test-only) | [architecture.md](architecture.md) |
| 2026-06-30 | trinity-enterprise#58 | feat(ui): Brain Orb — capability-gated per-agent 3D knowledge-graph page. Phase 1 (static render: first-party CSP-clean assets + same-origin iframe host + read-only `data.json` proxy) + Phase 2 (live scope mount/unmount → owner-gated agent re-export via `~/.trinity/brain-orb/` convention hooks → in-place rebuild). Voice/KB-actions/transcript deferred | [brain-orb.md](feature-flows/brain-orb.md) |
| 2026-06-29 | #1376 | fix(session): reconcile against server state on a severed turn so long Session turns never show a false "Failed to send" | [session-tab.md](feature-flows/session-tab.md) |
| 2026-06-29 | #506 | feat(capacity): admin fleet-wide `max_parallel_tasks` ceiling + runtime clamp + owner/admin UI | [capacity-management.md](feature-flows/capacity-management.md) |
| 2026-06-29 | #894 | feat(channels): per-agent **public-channel model override** — owners pick the Claude model for public-facing chats (public link, Slack/Telegram/WhatsApp, x402) via the Sharing tab; additive NULL `agent_ownership.public_channel_model` (NULL→platform default), owner-only `GET`/`PUT /api/agents/{name}/public-channel-model`. Epic #1079. | [public-channel-model.md](feature-flows/public-channel-model.md) |
| 2026-06-29 | #846 | feat(mcp): per-agent opt-in exposure as a dedicated `chat_with_<slug>` tool (dynamic register, poll-reconciled) | [mcp-agent-exposure.md](feature-flows/mcp-agent-exposure.md) |
| 2026-06-28 | #1155 | feat(loops): per-loop cost budget (`max_cost_usd`) iteration-boundary hard stop | [run-agent-loop.md](feature-flows/run-agent-loop.md) |
| 2026-06-28 | #1157 | feat(reliability): no-progress / doom-loop detection for sequential loops | [run-agent-loop.md](feature-flows/run-agent-loop.md) |
| 2026-06-28 | #1085 | feat(reliability): correlated-failure / thundering-herd controls for re-delivery | [redelivery-governor.md](feature-flows/redelivery-governor.md) |
| 2026-06-27 | #918 | feat: agent-reported structured reports via MCP + dashboard | [agent-reports.md](feature-flows/agent-reports.md) |
| 2026-06-27 | #905 | feat(mcp): deterministic git tools over MCP + request-id audit correlation | [mcp-git-tools.md](feature-flows/mcp-git-tools.md), [audit-trail.md](feature-flows/audit-trail.md) |
| 2026-06-27 | #792 | feat(reliability): retry the triggering execution after a SUB-003 auto-switch | [task-execution-service.md](feature-flows/task-execution-service.md) |
| 2026-06-26 | #1332 | fix(reliability): record a user-cancelled execution's activity as CANCELLED | [execution-termination.md](feature-flows/execution-termination.md), [dashboard-timeline-view.md](feature-flows/dashboard-timeline-view.md) |
| 2026-06-26 | #1131 | fix(deploy): probe in-container docker.sock GID for non-root backend (macOS/Colima/rootless) | [docker-socket-gid-detection.md](feature-flows/docker-socket-gid-detection.md) |
| 2026-06-23 | #654 | refactor(models): centralize router Pydantic models in `models.py` (Invariant #14) | [architecture.md](architecture.md) |
| 2026-06-22 | trinity-enterprise#38, #82 | feat(setup): first-run operator intake + admin email login | [first-time-setup.md](feature-flows/first-time-setup.md) |
| 2026-06-22 | #1084 | feat(reliability): effect-scoped idempotency for outbound side effects | [effect-idempotency.md](feature-flows/effect-idempotency.md) |
| 2026-06-21 | #1159 | fix(security): authenticate the in-container agent server on the shared network | [agent-server-authentication.md](feature-flows/agent-server-authentication.md) |
| 2026-06-21 | #946 | feat(orchestration): pull-pilot routing for agent→agent MCP chat | [agent-to-agent-collaboration.md](feature-flows/agent-to-agent-collaboration.md), [mcp-orchestration.md](feature-flows/mcp-orchestration.md) |
| 2026-06-21 | #1088 | refactor(reliability): unify the SUB-003 auth-class failure classifier | [subscription-auto-switch.md](feature-flows/subscription-auto-switch.md) |
| 2026-06-20 | #1169 | feat(infra): agent runtime data — declared `data_paths` + export/import | [agent-data-volumes.md](feature-flows/agent-data-volumes.md) |
| 2026-06-20 | #668 | feat: agent deployment compatibility validation | [agent-compatibility-validation.md](feature-flows/agent-compatibility-validation.md) |
| 2026-06-19 | #1231 | fix(agent): agent `/tmp` tmpfs size configurable via `AGENT_TMP_SIZE` | [container-capabilities.md](feature-flows/container-capabilities.md) |
| 2026-06-19 | #1115 | feat(ui): per-schedule performance scorecards on Agent Detail | [agent-overview-dashboard.md](feature-flows/agent-overview-dashboard.md), [scheduling.md](feature-flows/scheduling.md) |
| 2026-06-19 | #1083 | feat(exec): fire-and-forget dispatch (202 ACK + result callback) | [task-execution-service.md](feature-flows/task-execution-service.md), [capacity-management.md](feature-flows/capacity-management.md), [cleanup-service.md](feature-flows/cleanup-service.md) |

---

## Documented Flows

### Core Agent Features

| Flow | Document | Description |
|------|----------|-------------|
| Agent Lifecycle | [agent-lifecycle.md](feature-flows/agent-lifecycle.md) | Create, start, stop, delete Docker containers |
| Ephemeral Agents | [ephemeral-agents.md](feature-flows/ephemeral-agents.md) | Budgeted "ghost" agents: hard-discard lifecycle, budget gates, GC, ghost-key fence, spawn provenance + parent control (trinity-enterprise#69) |
| Default Cornelius Agent | [cornelius-default-agent.md](feature-flows/cornelius-default-agent.md) | Auto-seed a default Cornelius second-brain agent + enable the Brain Orb on fresh install; first-run-only, fresh-install-scoped `ensure_seeded()` (trinity-enterprise#107) |
| Agent Rename | [agent-rename.md](feature-flows/agent-rename.md) | Rename agents via UI, MCP, or API (RENAME-001) |
| Agent Terminal | [agent-terminal.md](feature-flows/agent-terminal.md) | Browser-based xterm.js terminal with Claude/Gemini/Bash modes |
| Credential Injection | [credential-injection.md](feature-flows/credential-injection.md) | CRED-002: Direct file injection, encrypted git storage |
| Agent Scheduling | [scheduling.md](feature-flows/scheduling.md) | Cron-based automation with APScheduler |
| Webhook Triggers | [webhook-triggers.md](feature-flows/webhook-triggers.md) | Token-authenticated public URL to fire schedule executions (WEBHOOK-001) |
| Scheduler Service | [scheduler-service.md](feature-flows/scheduler-service.md) | Standalone scheduler with Redis distributed locks |
| Execution Queue | [execution-queue.md](feature-flows/execution-queue.md) | Redis-based parallel execution prevention |
| Execution Termination | [execution-termination.md](feature-flows/execution-termination.md) | Stop running executions via process registry |
| Parallel Headless Execution | [parallel-headless-execution.md](feature-flows/parallel-headless-execution.md) | Stateless parallel task execution via POST /task |
| Parallel Capacity | [parallel-capacity.md](feature-flows/parallel-capacity.md) | Per-agent parallel execution slot tracking |
| Persistent Task Backlog | [persistent-task-backlog.md](feature-flows/persistent-task-backlog.md) | SQLite-backed FIFO backlog for async tasks at capacity (BACKLOG-001) |
| Capacity Management | [capacity-management.md](feature-flows/capacity-management.md) | Unified facade for per-agent execution capacity (#428) |
| Task Execution Service | [task-execution-service.md](feature-flows/task-execution-service.md) | Unified execution lifecycle for all task callers (EXEC-024) |
| Idempotency Keys | [idempotency-keys.md](feature-flows/idempotency-keys.md) | `Idempotency-Key` dedup at every execution trigger boundary — one execution per `(scope,key)` in 24h, fail-open (RELIABILITY-006, #525, Invariant #18) |
| Effect Idempotency | [effect-idempotency.md](feature-flows/effect-idempotency.md) | Per-sink exactly-once-style guard for outbound side effects (messages/voip/share_file/Nevermined settle) — `effect_guard` keyed on resolved identity, scoped by execution_id (#1084, Direction A — the pull-mode side-effect approach was reframed to retry-with-trace (#1401) + tool-side gates + async operator human-gate (#1402) in `TARGET_ARCHITECTURE.md` v2; gates per-effect, not *the* per-agent gate) |
| Redelivery Governor | [redelivery-governor.md](feature-flows/redelivery-governor.md) | Correlated-failure / thundering-herd controls for the #1083 re-delivery callback path — agent-side jitter (unflagged) + backend rate caps + distinct-agent shared-cause pause; fail-open, Redis-only, default-OFF behind `REDELIVERY_GOVERNOR_ENABLED` (#1085) |
| Business Validation | [business-validation.md](feature-flows/business-validation.md) | Post-execution auditor verifies task completion (VALIDATE-001) |
| Fan-Out | [fan-out.md](feature-flows/fan-out.md) | Parallel task dispatch and result collection via semaphore (FANOUT-001) |
| Sequential Agent Loops | [run-agent-loop.md](feature-flows/run-agent-loop.md) | `run_agent_loop` server-side sequential bounded task execution with stop-signal + graceful stop (#740) |
| Agent Self-Reminders | [agent-self-reminders.md](feature-flows/agent-self-reminders.md) | Durable one-shot deferred self-trigger — the scheduler arms a `DateTrigger` per pending reminder + fires a normal execution of the same agent (`triggered_by="reminder"`); self-scoped `set/list/cancel_reminder` (#1296) |
| Dispatch Circuit Breaker | [dispatch-circuit-breaker.md](feature-flows/dispatch-circuit-breaker.md) | Per-agent producer-side dispatch breaker (RELIABILITY-007, #526) |
| Execution Context Injection | [execution-context-injection.md](feature-flows/execution-context-injection.md) | Inject prior-execution context into a turn (#171) |
| Schedule Pre-Check | [scheduler-pre-check.md](feature-flows/scheduler-pre-check.md) | Conditional template-supplied pre-check hook before a cron run (SCHED-COND-001, #454) |

### Dashboard & Monitoring

| Flow | Document | Description |
|------|----------|-------------|
| Agent Network (Dashboard) | [agent-network.md](feature-flows/agent-network.md) | Real-time visual graph at `/` |
| Dashboard Timeline View | [dashboard-timeline-view.md](feature-flows/dashboard-timeline-view.md) | Graph/Timeline mode toggle with execution boxes |
| Dashboard Grid View | [dashboard-grid-view.md](feature-flows/dashboard-grid-view.md) | Magnetic tile canvas dashboard mode (trinity-enterprise#47) |
| Dashboard List View | [dashboard-list-view.md](feature-flows/dashboard-list-view.md) | Agents-page row list as the third dashboard mode (trinity-enterprise#260) |
| Replay Timeline | [replay-timeline.md](feature-flows/replay-timeline.md) | Waterfall-style timeline visualization |
| Activity Stream | [activity-stream.md](feature-flows/activity-stream.md) | Centralized persistent activity tracking |
| Activity Monitoring | [activity-monitoring.md](feature-flows/activity-monitoring.md) | Real-time tool execution tracking |
| Agent Monitoring (Health) | [agent-monitoring.md](feature-flows/agent-monitoring.md) | Fleet-wide health checks (MON-001) |
| Agent Heartbeat Liveness | [agent-heartbeat-liveness.md](feature-flows/agent-heartbeat-liveness.md) | Push-based 5s liveness layer, watch loop + soft alerts (RELIABILITY-004 / #307) |
| Subscription Credential Health | [subscription-credential-health.md](feature-flows/subscription-credential-health.md) | Credential health monitoring, auto-remediation, alerts |
| Host Telemetry | [host-telemetry.md](feature-flows/host-telemetry.md) | Host CPU/memory/disk in Dashboard header |
| Agent Logs & Telemetry | [agent-logs-telemetry.md](feature-flows/agent-logs-telemetry.md) | Live metrics in AgentHeader |
| Agent Dashboard | [agent-dashboard.md](feature-flows/agent-dashboard.md) | Agent-defined dashboard via dashboard.yaml |
| Dynamic Dashboards | [dynamic-dashboards.md](feature-flows/dynamic-dashboards.md) | Historical widget values with sparklines (DASH-001) |
| Token Usage Display | [token-usage-display.md](feature-flows/token-usage-display.md) | Per-agent cost/token stats from DB in AgentHeader: sparkline, today vs 7-day avg trend (#250) |
| Executions Dashboard | [executions-dashboard.md](feature-flows/executions-dashboard.md) | Unified fleet executions list + stats (EXEC-022) |

### Agent Detail UI

| Flow | Document | Description |
|------|----------|-------------|
| Overview Tab | [agent-overview-dashboard.md](feature-flows/agent-overview-dashboard.md) | Default landing tab — multi-day trend charts + analytics endpoint (#1107) |
| Tab Overflow (More ▾) | [agent-detail-tab-overflow.md](feature-flows/agent-detail-tab-overflow.md) | Reusable `OverflowTabs.vue` — tabs collapse into a "More" dropdown instead of horizontal scroll (#1114) |
| Tasks Tab | [tasks-tab.md](feature-flows/tasks-tab.md) | Task execution UI with history |
| Playbooks Tab | [playbooks-tab.md](feature-flows/playbooks-tab.md) | Invoke agent skills from UI (PLAYBOOK-001) |
| Authenticated Chat Tab | [authenticated-chat-tab.md](feature-flows/authenticated-chat-tab.md) | Simple chat UI with dynamic status labels (CHAT-001, THINK-001) |
| Playbook Autocomplete | [playbook-autocomplete.md](feature-flows/playbook-autocomplete.md) | Slash-command autocomplete for playbooks in chat input |
| Voice Chat + Workspace | [voice-chat.md](feature-flows/voice-chat.md) | Voice conversations via Gemini Live API; Workspace mode with canvas panel tools (BETA) |
| Execution Log Viewer | [execution-log-viewer.md](feature-flows/execution-log-viewer.md) | Modal for viewing execution transcripts |
| Execution Detail Page | [execution-detail-page.md](feature-flows/execution-detail-page.md) | Dedicated page for execution details |
| Continue Execution as Chat | [continue-execution-as-chat.md](feature-flows/continue-execution-as-chat.md) | Resume executions as interactive chat (EXEC-023) |
| Agent Avatars | [agent-avatars.md](feature-flows/agent-avatars.md) | AI-generated avatars with reference images, emotion variants, and default generation (AVATAR-001/002/003) |
| Agent Info Display | [agent-info-display.md](feature-flows/agent-info-display.md) | Info tab: About leads; `template.yaml` metadata behind a collapsible "Technical details" (#1107) |
| Per-Agent File Manager | [file-browser.md](feature-flows/file-browser.md) | Two-panel file manager in Agent Detail Files tab |
| File Manager (Deprecated) | [file-manager.md](feature-flows/file-manager.md) | Former standalone `/files` page — replaced by per-agent Files tab |

### Collaboration & Permissions

| Flow | Document | Description |
|------|----------|-------------|
| Agent-to-Agent Collaboration | [agent-to-agent-collaboration.md](feature-flows/agent-to-agent-collaboration.md) | Inter-agent communication via MCP |
| Agent Event Subscriptions | [agent-event-subscriptions.md](feature-flows/agent-event-subscriptions.md) | Lightweight pub/sub for inter-agent event pipelines |
| Agent Evaluations | [agent-evaluations.md](feature-flows/agent-evaluations.md) | Referee surface for the `quality` axis + the completion relabel; the graded agent cannot write its own grade (ent#206) |
| Task Completion Events | [task-completion-events.md](feature-flows/task-completion-events.md) | Backend-emitted `agent.task.completed`/`failed` at every CAS-won terminal (#1578) |
| Channel Completion Report-Back | [channel-completion-report.md](feature-flows/channel-completion-report.md) | Delegated/background terminals report into the originating Slack/Telegram chat — binding-agent consent + delivery, effect-guarded (ent#224/ent#265) |
| Agent Permissions | [agent-permissions.md](feature-flows/agent-permissions.md) | Agent communication permissions |
| Agent Sharing | [agent-sharing.md](feature-flows/agent-sharing.md) | Cross-channel email allow-list (web/Slack/Telegram) with access policy and pending requests |
| Agent Shared Folders | [agent-shared-folders.md](feature-flows/agent-shared-folders.md) | File collaboration via shared volumes |
| Outbound File Sharing | [file-sharing-outbound.md](feature-flows/file-sharing-outbound.md) | Agents publish files to public download URLs (FILES-001) |
| Agent Tags & System Views | [agent-tags.md](feature-flows/agent-tags.md) | Tagging and saved filters (ORG-001) |
| Tag Clouds | [tag-clouds.md](feature-flows/tag-clouds.md) | Visual grouping on Dashboard |

### Authentication & Security

| Flow | Document | Description |
|------|----------|-------------|
| Email Authentication | [email-authentication.md](feature-flows/email-authentication.md) | Passwordless email login |
| Admin Login | [admin-login.md](feature-flows/admin-login.md) | Password-based admin auth |
| First-Time Setup | [first-time-setup.md](feature-flows/first-time-setup.md) | Admin password wizard |
| MCP API Keys | [mcp-api-keys.md](feature-flows/mcp-api-keys.md) | API key management |
| Execution Origin Tracking | [AUDIT-001-execution-origin-tracking.md](feature-flows/AUDIT-001-execution-origin-tracking.md) | Track who triggered executions |
| Agent-Server Authentication | [agent-server-authentication.md](feature-flows/agent-server-authentication.md) | Per-agent inbound auth for the in-container agent server (`:8000`) — derived `X-Trinity-Agent-Token` (HMAC over `AGENT_AUTH_SECRET`) enforced by a pure-ASGI middleware on every HTTP/WS route (#1159) |
| 4-Tier Role Model | [role-model.md](feature-flows/role-model.md) | user < operator < creator < admin role hierarchy (ROLE-001) |
| Guided Credential Setup | [guided-credential-setup.md](feature-flows/guided-credential-setup.md) | Per-agent credential checklist: what an agent needs, what is set (bounded `docker exec` probe, names never values), and where to get each — owner-only + human-only (ent#127) |

### Public Access & Monetization

| Flow | Document | Description |
|------|----------|-------------|
| Public Agent Links | [public-agent-links.md](feature-flows/public-agent-links.md) | Shareable public links: chat type only (SITE-001 reverse-proxy retired in #865; SITE-002 redesign pending) |
| Slack Integration | [slack-integration.md](feature-flows/slack-integration.md) | Slack as delivery channel for public links (SLACK-001) |
| Slack Channel Routing | [slack-channel-routing.md](feature-flows/slack-channel-routing.md) | Channel adapter abstraction + multi-agent Slack routing (SLACK-002) |
| Slack File Sharing | [slack-file-sharing.md](feature-flows/slack-file-sharing.md) | Inbound file uploads: images via vision, text via container (SLACK-FILES) |
| Telegram Integration | [telegram-integration.md](feature-flows/telegram-integration.md) | Per-agent Telegram bots with webhook transport, group chat support, and `/login` email verification (TELEGRAM-001, TGRAM-GROUP) |
| Unified Channel Access Control | [unified-channel-access-control.md](feature-flows/unified-channel-access-control.md) | Cross-channel access gate keyed on verified email — policy, /login, access requests (#311) |
| VoIP Telephony | [voip-telephony.md](feature-flows/voip-telephony.md) | Outbound phone calls over the Gemini Live bridge via Twilio Media Streams; per-agent voice binding, ticket-authed WS, post-call transcript processing. Flag-gated default OFF (VOIP-001, #1056) |
| OpenAI Codex Runtime | [codex-runtime.md](feature-flows/codex-runtime.md) | Third agent runtime ("harness == runtime") — `codex exec` engine with full safety parity (system prompt, read-only sandbox, guardrails, sanitization), `RuntimeCapabilities`, Session-tab gate, MCP via config.toml. See also the [Harness Authoring Guide](harness-authoring-guide.md) (#1187) |
| Generic ACP Runtime | [acp-runtime.md](feature-flows/acp-runtime.md) | Harness-neutral Agent Client Protocol runtime with trusted derived-image launchers, conservative capability gates, model propagation, fork-safe image CI, and Hermes/DeepSeek live acceptance |
| Nevermined x402 Payments | [nevermined-payments.md](feature-flows/nevermined-payments.md) | Per-agent paid API via x402 payment protocol (NVM-001) |
| WhatsApp Integration | [whatsapp-integration.md](feature-flows/whatsapp-integration.md) | Per-agent WhatsApp via Twilio (WHATSAPP-001) |

### Mobile & PWA

| Flow | Document | Description |
|------|----------|-------------|
| Mobile Admin PWA | [mobile-admin-pwa.md](feature-flows/mobile-admin-pwa.md) | Standalone mobile admin at `/m` with agent chat, autonomy toggle, Ops/System tabs (MOB-001) |

### Platform Services

| Flow | Document | Description |
|------|----------|-------------|
| Image Generation | [image-generation.md](feature-flows/image-generation.md) | Gemini-powered two-step image generation pipeline (IMG-001) |
| In-App Bug Reporting | [in-app-bug-reporting.md](feature-flows/in-app-bug-reporting.md) | Bug / feature / feedback reporting from the Help widget (#1116) |
| Docs Q&A | [trinity-docs-qa.md](feature-flows/trinity-docs-qa.md) | Documentation Q&A bot (Vertex AI Search) |

### MCP & Integration

| Flow | Document | Description |
|------|----------|-------------|
| MCP Orchestration | [mcp-orchestration.md](feature-flows/mcp-orchestration.md) | 62 MCP tools for agent orchestration |
| MCP Git Tools | [mcp-git-tools.md](feature-flows/mcp-git-tools.md) | Deterministic git tools over MCP + request-id audit correlation (#905) |
| MCP Agent Exposure | [mcp-agent-exposure.md](feature-flows/mcp-agent-exposure.md) | Per-agent opt-in dedicated `chat_with_<slug>` MCP tool, dynamically poll-reconciled (#846) |
| MCP Connector | [mcp-connector.md](feature-flows/mcp-connector.md) | Per-agent MCP connector — expose playbooks as tools to an external AI client via a scoped key; OSS-core (ent#46 → #118) |
| Agent MCP Key | [agent-mcp-key.md](feature-flows/agent-mcp-key.md) | The agent's own `scope='agent'` key — container config-truth probe, start-time drift self-heal, owner-driven rotation (#1854) |
| A2A Inbound Server | [a2a-inbound-server.md](feature-flows/a2a-inbound-server.md) | Opt-in public Agent Card + JSON-RPC/SSE task endpoint so external orchestrators discover and task an agent; per-caller `messageId` dedup, rate-limited public route, allow-list seam (ent#157/#160) |
| A2A Outbound Calls | [a2a-outbound-call.md](feature-flows/a2a-outbound-call.md) | The calling half: a Trinity agent tasks an external A2A agent through an operator-registered endpoint (never a caller-supplied URL); call-time SSRF re-validation, connect-time IP pinning, same-origin card pin, `effect_guard` keyed on the conversation, default-OFF kill switch (#736) |
| Trinity CLI | [cli-tool.md](feature-flows/cli-tool.md) | Python Click CLI with multi-instance profiles, mirroring core MCP tools as shell commands |
| Trinity Connect | [trinity-connect.md](feature-flows/trinity-connect.md) | Local-remote agent sync via WebSocket |
| Write User Memory | [write-user-memory.md](feature-flows/write-user-memory.md) | Per-user memory write MCP tool (MEM-001, #888) |

### GitHub Integration

| Flow | Document | Description |
|------|----------|-------------|
| GitHub Sync | [github-sync.md](feature-flows/github-sync.md) | Source mode (pull-only) or Working Branch mode |
| GitHub Repo Initialization | [github-repo-initialization.md](feature-flows/github-repo-initialization.md) | Initialize GitHub sync for existing agents |
| Agent Repo Binding | [agent-repo-binding.md](feature-flows/agent-repo-binding.md) | Bind a live agent to a GitHub repo the user owns (ent#109) |
| GitHub Import Intents | [github-import-intents.md](feature-flows/github-import-intents.md) | fork / copy / clone create intents + inline compat check (ent#15) |
| Persistent-State Allowlist | [persistent-state-allowlist.md](feature-flows/persistent-state-allowlist.md) | `.trinity/persistent-state.yaml` primitive for reset-preserve-state (S4, #383) |
| Git Sync Health | [git-sync-health.md](feature-flows/git-sync-health.md) | Auto-sync heartbeat, dual ahead/behind, dashboard dot, `/api/fleet/sync-audit`, creation-time canonical `.gitignore` seed (#2069, readiness-gated) |

### Skills Management

| Flow | Document | Description |
|------|----------|-------------|
| Skills CRUD | [skills-crud.md](feature-flows/skills-crud.md) | Admin CRUD for platform skills |
| Skill Assignment | [skill-assignment.md](feature-flows/skill-assignment.md) | Owner assigns skills to agents |
| Skill Injection | [skill-injection.md](feature-flows/skill-injection.md) | Automatic injection on agent start |
| Skills on Agent Start | [skills-on-agent-start.md](feature-flows/skills-on-agent-start.md) | Detailed startup injection flow |
| MCP Skill Tools | [mcp-skill-tools.md](feature-flows/mcp-skill-tools.md) | 8 MCP tools for skill management |
| Skills Library Sync | [skills-library-sync.md](feature-flows/skills-library-sync.md) | GitHub repository sync |

### Notifications & Events

| Flow | Document | Description |
|------|----------|-------------|
| Agent Notifications | [agent-notifications.md](feature-flows/agent-notifications.md) | Agent-to-platform notifications (NOTIF-001) |
| Events Page UI | [events-page.md](feature-flows/events-page.md) | Consolidated into Operating Room Notifications tab |
| Operating Room | [operating-room.md](feature-flows/operating-room.md) | Unified operator command center: queue, notifications, resolved (OPS-001); async fire-and-park contract + derived request ids + lease-reaper poison-park items (#1402); agent-authored ingestion depth/rate/size caps + reserved-id guard + leader lock (#1632); per-(agent, type) budget for agent-influenceable platform emitters + caller-parity guard (#1677) |
| Proactive Messaging | [proactive-messaging.md](feature-flows/proactive-messaging.md) | Proactive agent-to-user messaging (#321) |

### Configuration & Settings

| Flow | Document | Description |
|------|----------|-------------|
| Public-Channel Model | [public-channel-model.md](feature-flows/public-channel-model.md) | Per-agent model override for public-facing channels (#894) |
| Autonomy Mode | [autonomy-mode.md](feature-flows/autonomy-mode.md) | Agent autonomous operation toggle |
| AutonomyToggle Component | [autonomy-toggle-component.md](feature-flows/autonomy-toggle-component.md) | Reusable Vue toggle component |
| Read-Only Mode | [read-only-mode.md](feature-flows/read-only-mode.md) | Code protection via hooks (CFG-007) |
| Agent Guardrails | [agent-guardrails.md](feature-flows/agent-guardrails.md) | Baseline bash/path deny-lists, credential output scanner, turn/timeout/tool budgets; owner-only narrow overrides (GUARD-001/002/003) |
| Agent Resource Allocation | [agent-resource-allocation.md](feature-flows/agent-resource-allocation.md) | Per-agent memory/CPU limits + system-wide admin defaults (RES-001) |
| Container Capabilities | [container-capabilities.md](feature-flows/container-capabilities.md) | Full capabilities mode |
| Model Selection | [model-selection.md](feature-flows/model-selection.md) | LLM model selection for terminal, tasks, and schedules |
| Agent Quotas | [agent-quotas.md](feature-flows/agent-quotas.md) | Per-role agent creation limits (QUOTA-001) |
| Platform Settings | [platform-settings.md](feature-flows/platform-settings.md) | Admin settings page |
| Template Registry | [template-registry.md](feature-flows/template-registry.md) | Remote `registry.yaml` as the runtime source for the GitHub half of the template catalog, with the bundled list as its fail-open floor (TMPL-002, trinity-enterprise#14) |
| SSH Access | [ssh-access.md](feature-flows/ssh-access.md) | Ephemeral SSH credentials |
| Subscription Management | [subscription-management.md](feature-flows/subscription-management.md) | Claude Max/Pro subscription tokens via env var (SUB-002) |
| Subscription Usage Tracking | [subscription-usage-tracking.md](feature-flows/subscription-usage-tracking.md) | Rolling 5h/7d token and cost usage per subscription (SUB-004) |

### System & Infrastructure

| Flow | Document | Description |
|------|----------|-------------|
| Internal System Agent | [internal-system-agent.md](feature-flows/internal-system-agent.md) | Platform operations manager (trinity-system) |
| System Manifest | [system-manifest.md](feature-flows/system-manifest.md) | Recipe-based multi-agent deployment |
| System-Wide Trinity Prompt | [system-wide-trinity-prompt.md](feature-flows/system-wide-trinity-prompt.md) | Admin-configurable prompt injection |
| Vector Logging | [vector-logging.md](feature-flows/vector-logging.md) | Centralized log aggregation |
| OpenTelemetry Integration | [opentelemetry-integration.md](feature-flows/opentelemetry-integration.md) | OTel metrics export |
| Async Docker Operations | [async-docker-operations.md](feature-flows/async-docker-operations.md) | Non-blocking Docker SDK wrappers |
| Backend Image Packaging Guard | [backend-image-packaging.md](feature-flows/backend-image-packaging.md) | Dockerfile `COPY` glob + `backend-image-smoke.yml` boot-of-baked-prod-image CI — closes the source→image packaging gap that crash-looped the backend (#1033) |
| Docker Socket GID Detection | [docker-socket-gid-detection.md](feature-flows/docker-socket-gid-detection.md) | `start.sh` probes the in-container `docker.sock` GID so the non-root backend reaches Docker on Docker Desktop/Colima/rootless + throttled WARN on denial + hermetic CI guard — closes the #874 "No agents" regression (#1131) |
| Cleanup Service | [cleanup-service.md](feature-flows/cleanup-service.md) | Active watchdog reconciliation + passive stale recovery for executions, activities, and slots (CLEANUP-001, #129) |
| Automatic Database Backups | [database-backup.md](feature-flows/database-backup.md) | Daily 03:30 UTC in-process backup for SQLite (`Connection.backup()`) and PostgreSQL (`pg_dump -Fc`) under `/data/backups/`, boot pre-migration copy, inverted-fail-safe retention (`backup_retention_days` + fixed `MIN_KEEP=3` floor), durable status + edge/staleness operator alarms, exercised restore (#2216) |
| Database Migration Runner | [database-migration-runner.md](feature-flows/database-migration-runner.md) | Cross-process `flock` + atomic rename-swap rebuilds make the SQLite migration suite crash-safe and concurrency-safe; failed migration named in the `/health` 503 (#1160 / #1183) |
| WebSocket Event Bus | [websocket-event-bus.md](feature-flows/websocket-event-bus.md) | Redis Streams transport for `/ws` + `/ws/events` with reconnect replay, per-client eviction, `MAXLEN` trim (RELIABILITY-003 / #306) |

### Templates & Pages

| Flow | Document | Description |
|------|----------|-------------|
| Template Processing | [template-processing.md](feature-flows/template-processing.md) | GitHub and local template handling |
| Library Page | [library-page.md](feature-flows/library-page.md) | `/library` — agent templates + fleet skills browse; legacy `/templates` redirects (trinity-enterprise#263) |
| API Keys Page | [api-keys-page.md](feature-flows/api-keys-page.md) | `/api-keys` page UI flow |
| Agents Page UI | [agents-page-ui-improvements.md](feature-flows/agents-page-ui-improvements.md) | **Superseded** (trinity-enterprise#260) — page retired into the Dashboard List mode; see [dashboard-list-view.md](feature-flows/dashboard-list-view.md) |
| Alerts Page | [alerts-page.md](feature-flows/alerts-page.md) | Removed in #430 (process engine deletion; cost alerts were PE-only) |

### File Management

| Flow | Document | Description |
|------|----------|-------------|
| Web Chat File Upload | [web-chat-file-upload.md](feature-flows/web-chat-file-upload.md) | Drag-drop/picker for authenticated and public chat; shared upload_service (#364) |

### Chat & Sessions

| Flow | Document | Description |
|------|----------|-------------|
| Persistent Chat Tracking | [persistent-chat-tracking.md](feature-flows/persistent-chat-tracking.md) | Database-backed chat persistence |
| Session Tab | [session-tab.md](feature-flows/session-tab.md) | The `--resume` engine — each turn reattaches to the same Claude memory (SESSION_TAB_2026-04). Its Agent Detail surface retired in ent#358; the engine and endpoints are live |
| Workspace absorbs Session | [workspace-absorbs-session.md](feature-flows/workspace-absorbs-session.md) | ent#358 — one continuous-conversation surface, with the continuity parity that had to land before the other could be removed |
| Workspace sidebar IA | [workspace-sidebar-ia.md](feature-flows/workspace-sidebar-ia.md) | ent#359 — agents block on top, starred chats, per-agent unread badges; why the per-viewer state could not be a column on the chat row |
| Workspace agent page | [workspace-agent-page.md](feature-flows/workspace-agent-page.md) | ent#360 — an agent becomes a destination; what the page deliberately does NOT carry, and why that is enforced by projection rather than by template |
| Workspace composer typeahead | [workspace-composer-typeahead.md](feature-flows/workspace-composer-typeahead.md) | ent#392 — `/` playbooks and `@` agents in the composer; why the trigger rule is stricter than the parser, why un-mentionable slugs are excluded, and why Enter never accepts implicitly |
| Workspace session identity | [workspace-session-signout.md](feature-flows/workspace-session-signout.md) | ent#357 + #2258 — two ways of being signed in, one way out; why sign-out destroys the credential rather than suppressing a derivation of it |
| Web Terminal | [web-terminal.md](feature-flows/web-terminal.md) | Browser-based terminal for System Agent |
| Self-Execute | [self-execute.md](feature-flows/self-execute.md) | Agent background task during chat (SELF-EXEC-001) |

### Testing & Development

| Flow | Document | Description |
|------|----------|-------------|
| Testing Agents Suite | [testing-agents.md](feature-flows/testing-agents.md) | Automated pytest suite (1460+ tests) |
| Local Agent Deployment | [local-agent-deploy.md](feature-flows/local-agent-deploy.md) | Deploy local agents via MCP |
| Dark Mode / Theme | [dark-mode-theme.md](feature-flows/dark-mode-theme.md) | Client-side theme system |

---

## Archived Flows

Preserved in `feature-flows/archive/` for historical reference.

| Flow | Status | Document | Reason |
|------|--------|----------|--------|
| Auth0 Authentication | REMOVED | [archive/auth0-authentication.md](feature-flows/archive/auth0-authentication.md) | Replaced by email auth (2026-01-01) |
| Agent Chat | DEPRECATED | [archive/agent-chat.md](feature-flows/archive/agent-chat.md) | Replaced by Agent Terminal |
| Agent Vector Memory | REMOVED | [archive/vector-memory.md](feature-flows/archive/vector-memory.md) | Templates should define their own |
| Agent Network Replay | SUPERSEDED | [archive/agent-network-replay-mode.md](feature-flows/archive/agent-network-replay-mode.md) | Replaced by Dashboard Timeline |
| System Agent UI | CONSOLIDATED | [archive/system-agent-ui.md](feature-flows/archive/system-agent-ui.md) | Uses regular AgentDetail.vue |
| Skills Management | SPLIT | — (document not preserved) | Split into the dedicated skills flows above (skill-assignment, skill-injection, skills-library-sync) |

---

## Requirements Specs

### Implemented

| Document | Status | Description |
|----------|--------|-------------|
| [DEDICATED_SCHEDULER_SERVICE.md](../requirements/DEDICATED_SCHEDULER_SERVICE.md) | ✅ | Standalone scheduler service |
| [EXTERNAL_PUBLIC_URL.md](../requirements/EXTERNAL_PUBLIC_URL.md) | ✅ | External URL for public links |
| [EXECUTION_ORIGIN_TRACKING.md](../requirements/EXECUTION_ORIGIN_TRACKING.md) | ✅ | Track who triggered executions |
| [AGENT_SYSTEMS_AND_TAGS.md](../requirements/AGENT_SYSTEMS_AND_TAGS.md) | ✅ | Tags and System Views |
| [NEVERMINED_PAYMENT_INTEGRATION.md](../requirements/NEVERMINED_PAYMENT_INTEGRATION.md) | ✅ | Per-agent x402 payment monetization |

### Pending

| Document | Priority | Description |
|----------|----------|-------------|
| [PUBLIC_EXTERNAL_ACCESS_SETUP.md](../requirements/PUBLIC_EXTERNAL_ACCESS_SETUP.md) | MEDIUM | Infrastructure setup for public access |

---

## Core Specifications

| Document | Purpose |
|----------|---------|
| [TRINITY_COMPATIBLE_AGENT_GUIDE.md](../TRINITY_COMPATIBLE_AGENT_GUIDE.md) | Creating Trinity-compatible agents |
| [MULTI_AGENT_SYSTEM_GUIDE.md](../MULTI_AGENT_SYSTEM_GUIDE.md) | Building multi-agent systems |

---

## Flow Document Template

Save flows to: `docs/memory/feature-flows/{feature-name}.md`

```markdown
# Feature: {Feature Name}

## Overview
Brief description of what this feature does.

## User Story
As a [user type], I want to [action] so that [benefit].

## Entry Points
- **UI**: `src/frontend/src/views/Component.vue` - Action trigger
- **API**: `METHOD /api/endpoint`

## Frontend Layer
### Components
- `Component.vue:line` - handler() method

### State Management
- `stores/store.js` - action name

## Backend Layer
### Endpoints
- `src/backend/routers/file.py:line` - endpoint_handler()

### Business Logic
1. Step one
2. Step two

## Data Layer
### Database Operations
- Query: Description
- Update: Description

## Side Effects
- WebSocket broadcast: `{type, data}`

## Error Handling
- Error case → HTTP status

## Testing
### Prerequisites
- Services running
- Test user logged in

### Test Steps
1. **Action**: Do X
   **Expected**: Y happens
   **Verify**: Check Z

## Related Flows
- [related-flow.md](feature-flows/related-flow.md)
```

---

## How to Create a Flow Document

1. Run `/feature-flow-analysis {feature-name}`
2. Or manually trace: UI → API → Backend → Database → Side Effects
3. Add Testing section with step-by-step verification
4. Update this index after creating

See `docs/TESTING_GUIDE.md` for testing template and examples.
